# Bordkasse für Segeltörns — Claude Code Projekt

> Tool zur fairen Aufteilung gemeinsamer Kosten auf Segeltörns mit wechselnden Crews.
> Zwei Implementationen parallel im Repo:
> - **Sheets-Lösung** (Apps Script v11) — pragmatisch, kein Login, eingefroren.
> - **Web-App** (`webapp/`) — Next.js + Supabase + Vercel, Crew-fähig, Magic-Link-Auth, Realtime, PWA-Offline, Statistik, Audit-Log, automatische DSGVO-Löschung 30 Tage nach Törnende.

## Quick Context

**Wer baut das?** Jannik, Skipper, mehrere Törns pro Jahr, bis zu 12 Crewmitglieder pro Törn, Crew teils nicht-technikaffin (Eltern, Freunde).

**Was wird gelöst?** Faire Kostenaufteilung mit Berücksichtigung von:
- Teilweiser Anwesenheit (Crewmitglieder steigen verschieden ein/aus)
- Verschiedenen Aufteilungslogiken pro Ausgabe (gleichmäßig, nur Anwesende, zeitanteilig, individuell)
- Alkohol-Anteilen (Nicht-Trinker zahlen nicht mit)
- Vorauszahlungen außerhalb der Bordkasse (Gutschriften)

**Zielsetzung:** Smartphone-tauglich, einfach genug für jedes Crewmitglied, kein Login-Aufwand.

## Aktueller Stand

Funktionierende Google-Sheets-Datei mit Apps Script. Dateien in `assets/sheets-current/`:
- `Bordkasse_IJsselmeer2026_v10.xlsx` — produktive xlsx (Layout v10, kompatibel mit Script v10 und v11)
- `Bordkasse_AppScript_v11.js` — produktiver Script-Code (manuell einzufügen)
- `Bordkasse_AppScript_v10.js` — Vorgänger-Script (Referenz)
- `Bordkasse_IJsselmeer2026_v9.xlsx` + `Bordkasse_AppScript_v9.js` — Vorgängerversion v9 (Referenz)
- `Bordkasse_IJsselmeer2026.xlsx` + `Bordkasse_AppScript_v8.js` — Vorgängerversion v8 (Referenz)

**Migrationen:** `scripts/migrate_v8_to_v9.py`, `scripts/migrate_v9_to_v10.py` — reproduzierbarer Umbau via openpyxl.

**Status:** Script-Version v11 (xlsx-Layout v10). Neu vs v10:
- Menüpunkt "🆕 Neuen Törn starten" — leert Transaktionen, Crew-Daten, Törn-Datum, Schulden, Eingabe + Gutschrift in einem Rutsch (Calc-Formeln bleiben)

## Working Language

Deutsch. Alle Domain-Begriffe sind deutsch und sollen so bleiben:
- *Bordkasse* — die gemeinsame Kasse
- *Törn* — eine Segelreise
- *Besatzung* / *Crew* — die Mitsegler
- *An Bord* — anwesend auf dem Schiff
- *Zeitanteilig* — proportional zur Anwesenheitsdauer
- *Gleichmäßig* — alle gleich
- *Gutschrift* — Verrechnung außerhalb der Bordkasse
- *Schulden* — wer wem wie viel zahlen muss
- *Bilanz* — Saldo pro Person
- *Aufteilung* — Methode, wie eine Ausgabe verteilt wird

## Architektur (aktuelle Sheets-Lösung)

### Tabs in der Excel-Datei (Reihenfolge wichtig)

1. **Eingabe** — Mobile-optimierte Eingabemaske für **Ausgaben** (Datum/Beschreibung/Kategorie/…/Aufteilung)
2. **Gutschrift** — eigenständige Maske für Gutschriften (seit v9)
3. **Besatzung** — Törn-Daten + Crewliste mit Anwesenheitszeiten
4. **Kategorien** — zentrale Pflege der Ausgabenkategorien (Single Source of Truth)
5. **Transaktionen** — alle Eingaben als Datensätze (Ausgaben + Gutschriften)
6. **Bilanz** — automatische Berechnung der Salden pro Person
7. **Schulden** — vereinfachter Zahlungs-Plan (per Apps Script befüllt)
8. **Auswertung** — Kategorien × Personen Matrix

### Eingabe-Tab Zellkoordinaten (kritisch für Apps Script)

```
B6   = Datum            (Autofill = heute)
B9   = Beschreibung
B12  = Kategorie        (Dropdown Kategorien!B4:B23)
B15  = Bezahlt von      (Dropdown Besatzung!B11:B22)
B18  = Betrag (€)
B21  = Alkohol-Anteil (€, optional)
B25  = Aufteilung       (Dropdown Gleichmäßig/An Bord/Zeitanteilig/Individuell)
B31–B42 = Crew-Namen (Formel-Lookup auf Besatzung!B11:B22)
C31–C42 = Anwesenheits-Checkboxen (P1–P12) für "Individuell"

Dynamische Bereiche:
- Zeilen 28–42: Individuell-Block (sichtbar nur wenn Aufteilung = "Individuell")
- Zeilen 31–42 einzeln: ausgeblendet wenn Besatzung!B(11+i) leer (live via onEdit)
```

### Gutschrift-Tab Zellkoordinaten

```
B6   = Datum            (Autofill = heute)
B9   = Beschreibung     (optional — leer → "Gutschrift" als Default-Eintrag)
B12  = Betrag (€)
B16  = Zahlt (Von)      (Dropdown Besatzung!B11:B22)
B19  = Empfängt (An)    (Dropdown Besatzung!K11:K23, inkl. "Alle")
```

### Besatzung-Tab Struktur

```
B5  = Törn-Start
B6  = Törn-Ende
B7  = Gesamttage (berechnet)

Crew-Tabelle (Zeilen 11–22, max. 12 Personen):
B = Name
C = An Bord ab
D = An Bord bis
E = Tage (berechnet)
F = Alkohol? (x für Trinker)
G = Zeitanteil % (berechnet)
H = Hinweis
K = Hilfsspalte für Gutschrift-"An"-Dropdown ("Alle" + Namen)
```

## Aufteilungs-Logiken (vier Varianten)

### 1. Gleichmäßig
Alle eingetragenen Crewmitglieder zahlen gleich viel — unabhängig von Anwesenheit.

```
Beispiel: 100€ Lebensmittel auf 10 Crew → jeder 10€
```

### 2. An Bord
Nur Personen, die am Tag der Ausgabe laut Besatzungs-Daten anwesend waren.

```
Beispiel: 80€ Restaurant am 08.04., Stephan kommt erst 10.04.
→ 9 Personen teilen 80€ → je 8,89€, Stephan 0€
```

### 3. Zeitanteilig
Proportional zur Anzahl Bord-Tage.

```
Beispiel: 210€ Sprit, 9 Personen à 11 Tage + Stephan 6 Tage = 105 Personentage
→ 11-Tage-Person: 22€, Stephan: 12€
```

### 4. Individuell
Nur explizit markierte Personen (Checkbox in der Eingabe).

```
Beispiel: 120€ Schwimmwesten für 3 Personen → je 40€
```

### Alkohol-Logik (modifiziert jede der 4 Aufteilungen)

Bei Ausgaben mit Alkohol-Anteil wird der Betrag aufgeteilt:
- Nicht-Alkohol-Teil: nach gewählter Aufteilung
- Alkohol-Teil: nur auf Personen mit "x" in Besatzung Spalte F (Alkoholtrinker)

```
Beispiel: 100€ Restaurant, 30€ davon Alkohol, "An Bord" alle 10 dabei, 3 Trinker
→ Nicht-Alkohol: 70€/10 = 7€ pro Person
→ Alkohol: 30€/3 = 10€ pro Trinker
→ Trinker: 17€, Andere: 7€
```

## Gutschrift-Logik

Gutschriften = Geld das außerhalb der Bordkasse fließt (z.B. Yacht-Vorauszahlung).

**Direkte Gutschrift** (Von Lucas → An Jannik, 240€):
- Lucas Saldo +240€ ("hat gegeben")
- Jannik Saldo -240€ ("hat erhalten")

**An Alle** (Von Lucas → An "Alle", 240€):
- Lucas hat 240€ direkt an die Crew gezahlt (z.B. seinen Yacht-Anteil nachträglich)
- Lucas: +240€
- Alle anderen Crewmitglieder bekommen anteilig zugerechnet: -240€/(N-1) je Person
- Saldo-Summe muss 0 bleiben

## Schulden-Algorithmus (Apps Script)

Greedy-Matching für minimale Anzahl Überweisungen:
1. Bilanz-Salden lesen
2. Schuldner (negativ) und Gläubiger (positiv) trennen, sortiert nach Höhe
3. Größter Schuldner zahlt an größten Gläubiger so viel wie möglich
4. Wenn einer "leer" ist, nächster Schuldner/Gläubiger
5. Maximal N-1 Überweisungen bei N Personen

## Design-System

Marineblau-dominiertes "Segel-Design", siehe `docs/design-system.md`.

Wichtigste Farben:
- `#114884` Primärblau (Texte, Linien, Rahmen)
- `#1D4281` abgedunkeltes Blau
- `#587EA8` mittleres Blau
- `#D6E1EE` helles Blau (Füllungen)
- `#FDF6DC` zartes Gold (Info-Boxen)
- Kein reines Schwarz

Schriften: Campton Bold (Display) → Arial Bold (H2) → Arial Regular (Body).

## Häufige Aufgaben für Claude Code

### Sheets-Lösung weiterentwickeln

**Neue Crewliste für nächsten Törn:**
- Datei in `assets/sheets-current/` öffnen
- Tab "Besatzung" Zeilen 11–22 anpassen (Name, An Bord ab/bis, Alkohol)
- Apps Script bleibt unverändert
- Wenn Personen gelöscht werden: ALLE Formelreferenzen prüfen (siehe "Bekannte Fallen")

**Apps Script erweitern:**
- Datei `Bordkasse_AppScript_v11.js` editieren
- Zellkoordinaten oben in Konstanten `E` (Eingabe), `G` (Gutschrift), `TX`, `B`, `S`
- Neue Validierungen in `transaktionSpeichern()` bzw. `gutschriftSpeichern()`
- Neue Aufteilungslogik: in Transaktionen-Sheet Spalten V–AG (P1-P12 Calc) Formel anpassen, dann Apps Script ggf. erweitern

**Neue Aufteilungsart hinzufügen:**
1. Eingabe-Tab Dropdown B25 erweitern
2. Calc-Formel in Transaktionen Spalten V–AG erweitern (verschachteltes IF)
3. Apps Script `transaktionSpeichern` ggf. mit neuer Validierung
4. Doku in `docs/calculation-rules.md` updaten

**Crew-Größe ändern (mehr/weniger als 12):**
- `B.N` im Apps Script anpassen
- Besatzung-Tab Zeilen 11–22 erweitern (alle Berechnungs-Spalten C–H)
- Eingabe-Tab Person-Zeilen 31–42 entsprechend erweitern (Formel + Checkbox-DV)
- `INDIVIDUELL_LAST_ROW`, `DABEI_COUNT` updaten
- Transaktionen Spalten V–AG (Calc) ggf. erweitern

**Strukturelle Änderungen am Tab-Layout:**
- Niemals manuell im xlsx — immer ein Migrations-Skript in `scripts/` schreiben (Vorlage: `scripts/migrate_v8_to_v9.py`)
- Skript erzeugt parallel `_vN.xlsx` + `_vN.js`, alte Version bleibt unangetastet
- openpyxl shiftet Merges + Data-Validations beim `delete_rows` NICHT automatisch — beide manuell behandeln (siehe Skript)

### Bekannte Fallen

**Beim Löschen einer Person aus Besatzung:**
- Spalte X (Person 3 Calc) bricht mit `#REF!`
- Eingabe-Labels in Zeilen 21–22 (alt) bzw. die mobile Liste nicht automatisch nachgezogen
- Bilanz-Spalte A (Name-Lookup) kann auf falsche Zeile zeigen
- **Fix:** Calc-Formel aus Nachbar-Spalte regenerieren (P2 → P3 mit `$12` → `$13`), siehe Pattern in v8

**Beim Verschieben von Spalten in Besatzung:**
- 2400+ Formeln in Transaktionen referenzieren `Besatzung!$B$11:$B$22` für Namen
- Falsche Spalten-Referenz brechen die gesamte Datei lautlos (Bilanz zeigt "00:00:00" o.ä.)
- **Fix:** Niemals manuell Spalten in Besatzung verschieben — immer komplett neu formatieren

**MATCH-Falle bei gleichen Saldenbeträgen:**
- `MATCH` gibt immer ersten Treffer zurück → wenn 9 Personen je -10€ schulden, bleibt nur die erste sichtbar
- **Fix:** Im Apps Script `schuldenBerechnen_` werden alle Schuldner einzeln behandelt, kein MATCH-Lookup mehr

## Web-App `webapp/`

**Tech-Stack:** Next.js 16 (App Router, Turbopack) + Tailwind 4 + Supabase (Postgres + Auth + Realtime) + Vercel.

**Setup:** siehe `webapp/README.md`. Lokal: `cd webapp && pnpm install && supabase start && pnpm dev`. Magic-Link-Mails landen lokal im Mailpit unter http://127.0.0.1:54324.

**Aktueller Funktionsumfang:**

- **Auth:** Magic-Link per E-Mail (Token-Hash-Flow, Single-Use, 60 Min TTL).
  - Whitelist-Check vor dem Versand: nur E-Mails in `ADMIN_EMAILS`-Env oder bereits in `persons_private` (per Skipper-Einladung) bekommen einen Link — Fremde werden mit Fehlermeldung abgewiesen, keine auth.users-Leichen.
  - Klick auf den Magic-Link landet auf `/auth/confirm` (Server Page mit „Jetzt einloggen"-Button) → POST nach `/auth/verify` → `verifyOtp` + Session. Die Zwischenseite schützt gegen Link-Scanner (Outlook Safe Links etc.), die den Token sonst per Vorab-GET verbrauchen würden.
  - Bei abgelaufenem / verbrauchtem Link redirected `/auth/verify` zurück nach `/login?auth_error=…&email=…`; die Login-Page bietet direkt einen „Neuen Link an X senden"-Button (E-Mail-Adresse wird per `&email={{ .Email }}`-Template-Parameter durchgereicht).
  - Resend-Button nach 30 s im normalen Login-Flow.
- **Rollen:** Admin (`ADMIN_EMAILS`-Env), Original-Skipper (`trips.skipper_id`), Co-Skipper (`trip_members.is_skipper`), Crew-Member. Admin kann Törns für Freunde anlegen, ohne selbst in der Crew zu landen.
- **Trips:** CRUD inkl. Archivierung; Admin sieht ALLE Trips (Service-Role-Read-Bypass via `lib/supabase/read-client.ts`), auch fremde — kein 404 beim Drauflicken.
- **Reise-Typ „Andere Reise" (Migration 0039 = `trips.trip_type`, `sailing`|`other`, Default `sailing`):** Ein Törn kann als „Andere Reise" (z. B. Gruppen-Urlaub) markiert werden — Radio in der Anlage (`new-trip-form.tsx`, reagiert live auf die Auswahl) + Settings-Sektion (`trip-type-section.tsx` → `updateTripType` in `lib/actions/trips.ts`). Konsequenzen: (a) **Ausschluss aus der Gesamtstatistik** — `getGlobalStats` filtert auf `trip_type='sailing'` (live + gepurgt; Pro-Törn-Statistik bleibt); (b) **typabhängige Default-Kategorien** bei der Anlage (`defaultCategoriesFor` in `trips.ts` — „other" ohne Yacht/Sprit/Hafen, dafür Unterkunft/Transport); (c) **durchgängig neutrales Wording INNERHALB einer Reise** über den zentralen Helper `lib/trip-vocab.ts` (`tripVocab(tripType)`): Törn→Reise, Bordkasse→Urlaubskasse, Crew→Reisegruppe, An Bord→Anwesend, Koje→Zimmer, Skipper→Reiseleitung (geschlechtsneutral), Schiffsname→Unterkunft, Yacht-/Charteranzahlung→Urlaubsanzahlung, Vercharterer/Charteragentur→Anbieter. **Verteilung des Vokabulars:** Client-Komponenten ziehen es aus einem React-Context (`components/trip-vocab-provider.tsx` → `useTripVocab()`, im Trip-Layout gemountet — kein Prop-Threading nötig); die vier schon vorher prop-getriebenen Komponenten (`crew-section`, `prepayments/matrix`, `prepayments/setup/wizard`, `prepayment-plan-section`) nutzen weiter ihren `tripType`-Prop; Server-Seiten rufen `tripVocab(trip.trip_type)` lokal (`getTrip`→`select("*")` enthält `trip_type`). Auch alle **E-Mail-Templates** sind typabhängig (die Sender laden `trip_type` und reichen es durch). **Scope-Grenze (bewusst):** Törn/Bordkasse wechseln NUR innerhalb einer Reise (`app/trips/[id]/**` + Mails) — NICHT auf Trip-Liste/Landing (`app/page.tsx`), `/about`, `/datenschutz`, `/kontakt`; dort bleiben „Törn"/„Bordkasse" als Produktbegriffe. Die Mail-Header-/Logo-Wortmarke „Bordkasse" bleibt ebenfalls (Markenidentität). Unverändert auch **„Vorstrecker/Streckt vor"**.
- **Privacy-Split** (`persons` vs `persons_private`):
  - `persons.display_name` ist öffentlich (Vorname + ggf. Initial), darf NIE einen Nachnamen tragen.
  - `persons_private.last_name`, `persons_private.email` (CITEXT) — sichtbar nur für Self oder Trip-Skipper der eigenen Crew via RLS.
  - Service-Role-Schreibpfade laufen weiterhin über `createAdminClient()`.
- **Crew-Verwaltung:** Email-Einladung mit Ghost-Personen, Inline-Edit-Form (Name/Email nur für Ghosts editierbar; Anwesenheit/Alkohol/Notiz für alle).
  - **Auto-Invite-Mail:** beim Anlegen eines neuen Crew-Members wird automatisch ein Magic-Link verschickt — der Skipper muss nicht extra sagen „und jetzt geh auf /login". UPSERT-Updates (z.B. Anwesenheits-Edit) lösen KEINE Re-Invite-Mail aus. **Crash-fest:** Schlägt der Versand fehl, wird das Mitglied trotzdem angelegt und Crew-Maske/Anzahlungs-Wizard zeigen eine weiche Warnung (`MemberState.warning`) statt der Next-Error-Boundary — früher rief `inviteMember` `resolveOrigin` **ungekapselt** auf (anders als `updateMember`/`replaceMember`), was bei fehlendem Origin-Env die ganze Crew-Anlage mit Fehler-Digest abstürzen ließ, obwohl das Mitglied schon gespeichert war. **Name optional:** Bei „nur E-Mail, kein Name" leitet `displayNameFromEmail` (lib/utils.ts) den Anzeigenamen aus dem Teil vor dem @ ab — einheitlich in `inviteMember`, `createTrip`-Skipperpfad und `replaceMember` (`ReplaceMemberSchema` verlangt keinen Namen mehr; der `persons_private`-Insert dort ist jetzt fehlergeprüft).
  - **Remove-Schutz:** Crew-Member, die noch Buchungen haben (paid_by / credit_from / credit_to), können nicht entfernt werden — Skipper muss erst die Buchungen umbuchen.
- **Kategorien:** pro Trip mit lucide-react-Icon (kuratierte 23-Icon-Whitelist im Picker, `webapp/lib/categories/icons.ts`). Marineblau-monochrome Strich-Icons im Bottom-Nav-Stil. Default-Kategorien in dieser Reihenfolge: Lebensmittel→`ShoppingCart`, Restaurant→`Utensils`, Hafen / Liegeplatz→`Anchor`, Aktivitäten→`Ticket`, Ausrüstung→`Wrench`, Sprit→`Fuel`, Yacht→`Sailboat`, Versicherung→`ShieldCheck`, Kaution→`Banknote`, Sonstiges→`Package`. Render-Zeit-Fallback auf den Kategorie-Namen + Default-Icon `Tag` bei unbekannten Werten.
- **Buchungen:** alle 5 Aufteilungslogiken (Gleichmäßig, An Bord, Zeitanteilig, Individuell, **Pro Person** seit Migration 0014) + Alkohol-Modifikator (im UI unter „Erweitert" eingeklappt) + optionaler Tip-Betrag. Bei `per_person` werden die Einzelbeträge in `transaction_participants.amount` gespeichert; der Gesamtbetrag der Buchung ergibt sich aus der Summe. Currency-Input akzeptiert deutsches Komma. Idempotency-Key auf jeder Row gegen Doppelklick / Outbox-Replay. **Live-Vorschau:** unter den Aufteilungs-Tabs zeigt ein einklappbares `<details>` „Wer zahlt wie viel?" (default zu) live die Pro-Person-Anteile + Σ — gerechnet client-seitig mit demselben `lib/calc/shares.ts:calculateShares` wie die SQL-View, über alle 5 Modi inkl. Alkohol/Trinkgeld (`daysBetween` dafür nach `lib/utils.ts` extrahiert). **Struktur (entflochten):** `transaction-form.tsx` = Dispatcher + `ExpenseForm`/`CreditForm`; geteilte Bausteine in `transaction-form-parts.tsx` — v. a. der `useBookingSubmit`-Hook (Submit / Fat-Finger-Rückfrage / Offline-Enqueue / Draft-Überschreiben / Mid-Flight-Rettung / Fehler-Scroll — vorher in beiden Forms dupliziert) plus `FieldGroup`, `TrancheField`, `SharePreview`, `PerPersonAmounts`. **Datenintegrität:** die `transaction_participants`-Inserts/Deletes in `createExpense`/`updateExpense`/Replay sind fehlergeprüft — scheitert der Anteils-Write, wird die Buchung zurückgerollt (Delete) statt eines stillen „Erfolgs" mit fehlenden Anteilen. **Buchungsdatum frei:** das Datum darf vor/nach dem Törn liegen (Anzahlungstranchen, Versicherung, Nachzügler-Rechnung) — das Formular zeigt dann einen beruhigenden Hinweis statt zu blocken (kein `min`/`max` mehr am Datumsfeld). Einzige Ausnahme „An Bord": ist am Buchungstag niemand an Bord, bliebe die Ausgabe unallokiert (keine Shares, Bilanz-Summe ≠ 0) — abgefangen durch eine Warnung unter den Aufteilungs-Tabs UND serverseitig via `checkOnBoardDate` in `lib/actions/transactions.ts` (create + update + **Replay**). `checkOnBoardDate` deckt seit dem Review-Fund C-1 ZWEI Fälle ab: Datum außerhalb des Törnzeitraums **und** Datum innerhalb, aber vor dem ersten / nach dem letzten Anwesenheitsfenster der Crew (z. B. Charter-Übergabetag, an dem noch niemand `on_board_from` erreicht hat) — geprüft mit derselben `COALESCE(on_board_from, start_date) … COALESCE(on_board_to, end_date)`-Fensterlogik wie `v_transaction_shares` (0 erfasste Crew → nicht blocken, andere Guards greifen). Regressionstests in `__tests__/transaction-actions.test.ts`.
  - **Edit-Modus:** `/trips/[id]/transactions/[txId]/edit` — Skipper, Admin oder Ersteller (`created_by`) darf eine Buchung nachträglich ändern (Aufteilung, paid_by, Beträge, …). Pencil-Icon in der Buchungsliste neben dem Lösch-Button.
  - **Rechnen im Betragsfeld** (PR #192): Ausgabe-, Gutschrift-, Alkohol- und Trinkgeld-Feld akzeptieren Rechen-Ausdrücke (`47,30 - 6,00` für Privatkäufe, `240,00 / 4`). `onBlur` wertet via `safeMathEval` (`lib/utils/math-eval.ts`, CSP-sicher, kein eval) aus und schreibt das Ergebnis formatiert zurück; der Zod-Preprocessor `evalExpr` in `transaction-schema.ts` wertet serverseitig als Netz für Enter-/Autofill-Submits. `inputMode="text"` (statt `decimal`), sonst zeigt iOS keine Rechenzeichen.
  - **Fremdwährungen (Migration 0041):** Ein Törn kann Buchungen in einer anderen Währung als Euro haben. Der Skipper aktiviert die Währungen pro Törn in den Einstellungen (`settings/currency-section.tsx` → `updateTripCurrencies` in `lib/actions/trips.ts`; `trips.foreign_currencies TEXT[]`, leer = reiner Euro-Törn → **keine** Währungs-UI, unverändert). Kuratierte 21-Währungs-Liste in `lib/rates/currencies.ts` (DKK/SEK/NOK/GBP/CHF/PLN/BGN/ISK/TRY/ALL/USD/CAD/MXN/THB/XCD/BBD/BSD/DOP/MUR/SCR/XPF — alle vom Anbieter abgedeckt). **Kurs (Variante B — live je Buchung):** `lib/rates/get-rate.ts:getLiveRates` zieht server-seitig einen `open.er-api.com/latest/EUR`-Call (CSP `connect-src` blockt Client-Fetch → server-only; kostenlos, kein Key), invertiert zu „EUR pro 1 Einheit"; `lib/rates/currency-options.ts:getCurrencyOptions` reicht den Default-Kurs an die Buchungsseite (`new`+`edit`+DraftEditor). **Offline** greift der Kurs der letzten Buchung derselben Währung (aus `transactions.exchange_rate`); der Kurs ist im Formular immer editierbar (`CurrencyField` in `transaction-form-parts.tsx`, Kurs-Quelle `live`/`manual`/`bank`). **Damit auch die ERSTE Offline-Buchung einer Währung (ohne frühere Buchung) einen Kurs hat**, cacht das Formular online geladene Kurse persistent in localStorage (`lib/offline/rate-cache.ts`, `useCurrencyState`-Effekt); Fallback-Kette bei Währungswahl: Server-Default (live/letzte Buchung) → localStorage-Cache → leer (manuelle Eingabe). **EUR bleibt die Bilanz-Wahrheit:** bei Fremdwährung tragen die eingegebenen Beträge (amount/alkohol/tip/Pro-Person) den FREMDBETRAG; der Server (`resolveExpenseCurrency`/`resolveCreditCurrency` in `transactions.ts`) rechnet mit `foreignToEur` (`lib/rates/convert.ts`) in EUR um und legt `original_currency`/`original_amount`/`exchange_rate`/`rate_source` als Herkunft ab (Bon-Spur, in der Liste als „500,00 SEK" unter dem €-Betrag). **Pro Person + Fremdwährung:** jede Person trägt ihren Fremdbetrag vom Bon ein; `transaction_participants.original_amount` speichert den Fremdbetrag, `amount` den EUR-Wert. Views (`v_balances`, `v_transaction_shares`) unverändert — sie lesen nur `amount` (EUR). **Umrechnung in `lib/rates/resolve.ts`** (`resolveExpenseCurrency`/`resolveCreditCurrency`, pure & unit-getestet — NICHT in der `"use server"`-Datei, wo nur async-Exports erlaubt sind). **Tatsächlicher Bankkurs (Paket F):** im Bank-Block der Buchung (`CurrencyField`, Aufklapper „Echten Bankbetrag nachtragen") trägt man den real abgebuchten Euro-Betrag laut Kontoauszug ein (inkl. Gebühren); der Server leitet daraus den effektiven Kurs ab (`exchange_rate = abgebuchter Euro / Fremdbetrag`), setzt `rate_source='bank'` + `rate_confirmed_at` und die Bilanz nutzt den echten Betrag. **Divisor = voller Fremdbetrag der Kartenzahlung** (`bank_foreign_amount`, optionales Zweitfeld) — nur nötig, wenn im Betrag etwas rausgerechnet wurde (z. B. Privatkauf via Betragsrechner): dann bucht die Karte den ganzen Bon ab, aber die Buchung enthält nur den geteilten Teil; der Server rechnet `abgebuchter Euro / voller Fremdbetrag` und wendet den Kurs auf den geteilten Buchungsbetrag an. Leer → Divisor = Buchungsbetrag (kein Privatabzug). `bank_foreign_amount` ist transient (nicht gespeichert; beim Edit reproduziert der gespeicherte Kurs den Betrag). Client-Spiegel `computeEffRate` in `transaction-form.tsx` = `effectiveRate` im Server. Bei Pro Person wird der Betrag über den effektiven Kurs auf alle Anteile verteilt. Tests: `__tests__/currency.test.ts` (Umrechnung/Bank/Privatabzug/Schema), `__tests__/rate-cache.test.ts`. **Code-Review-Härtung Runde 2 (Tier 2):** (C-4) bei `rate_source='bank'` leitet `resolveExpenseCurrency`/`resolveCreditCurrency` den EUR-Betrag jetzt **centgenau** aus dem Bankbetrag ab (`bankExactEur` = `bankEur × Fremdbetrag / Divisor`) statt über den auf 6 Stellen gerundeten Kurs zurückzurechnen — sonst wich der gespeicherte Betrag bei großen Fremdbeträgen um 1 ct vom abgebuchten Euro ab (der `exchange_rate` bleibt als Herkunfts-Spur der 6-stellige Kurs). (C-5) die Pro-Person-Fremdwährungs-**Vorschau** (EUR-Gesamtbetrag + Fat-Finger) summiert jetzt die je Teilnehmer einzeln konvertierten Beträge (`eurTotal`), wie der Server — vorher konvertierte sie die Summe einmal und wich um Cents vom gebuchten Betrag ab. (O-3/C-2 siehe Tier D.)
- **Gutschriften** (direkt oder „An Alle") — nur Skipper/Admin. „An Alle" wird abgewiesen, wenn weniger als 2 Crewmitglieder dabei sind (sonst kann die Bilanz nicht ausgeglichen werden).
- **Bilanz** live aus `v_balances`.
- **Schulden** vereinfacht via `simplify_debts()`-Greedy. Bezahlt-Häkchen Crew-weit synchron in `settled_debts`; nur Schuldner, Gläubiger oder Admin dürfen togglen. Häkchen sind erst freigeschaltet, sobald der Skipper die Abrechnung verschickt hat (siehe Settlement-Workflow). **Bezahlt-Mail:** sobald jemand ein Häkchen setzt, gehen automatisch zwei Mails raus — an Schuldner UND Gläubiger. Der Wortlaut hängt vom Akteur ab (sechs Varianten, gesteuert über `actorRole: "debtor" | "creditor" | "other"` im Template): Schuldner-getriggert = „du hast deine Zahlung gemeldet" / „X hat seine Zahlung an dich abgehakt"; Gläubiger-getriggert = „X hat deine Zahlung als erhalten bestätigt" / „du hast den Empfang bestätigt"; Admin/Skipper-getriggert = neutrales „X hat das in der Bordkasse abgehakt". Beim Entfernen des Häkchens wird keine Mail verschickt. Template in `lib/email/debt-settled-template.ts`, Versand in `lib/actions/settled-debts.ts:sendDebtSettledMails` — Mail-Fehler brechen den Toggle nicht ab. `sendDebtSettledMails` liefert `{ sent, failed }` zurück, `toggleDebtSettled` reicht das durch; die Checkbox (`debt-checkbox.tsx`) zeigt per **globalem Client-Toast** (`useToast`) „Als bezahlt markiert — Crew benachrichtigt", bei `failed > 0` einen Teilfehler-Hinweis und bei `!ok` eine Fehlermeldung (statt stillem optimistischem Reset).
- **Settlement-Workflow:** Ab dem letzten Trip-Tag sehen Skipper/Co-Skipper/Admin einen Banner „Törn vorbei — Abrechnung verschicken?" auf Trip-Übersicht + Schulden-Seite. `announceSettlement` (in `lib/actions/settlement.ts`) berechnet aktuelle Bilanz + simplified-debts, schickt jedem Crewmitglied via SMTP/nodemailer eine personalisierte HTML-Mail (Saldo + Zahlungsplan + Link → Schulden-Tab), setzt `trips.settlement_announced_at`. Mail-Template-Design wie Magic-Link-Mail (Logo, Card auf #FAFBFC, table-Layout für Outlook). Idempotent: bereits angekündigte Trips können nicht erneut verschickt werden. Bei Edit/Delete einer Kaution-Buchung redirected die App auf `/trips/[id]?check_settlement=1` (Edit zusätzlich `&toast=expense-updated`, damit das Erfolgs-Feedback nicht verloren geht), was den Banner prominenter macht (border-2, Highlight-Text), damit der Skipper an die Abrechnung erinnert wird. `announceSettlement`/`resendSettlement` unterscheiden im Rückgabewert `skipped` (kein E-Mail-Postfach hinterlegt) von `failed` (Adresse vorhanden, Zustellung schlug fehl); die Buttons (`settlement-announce-button.tsx` / `settlement-resend-button.tsx`) zeigen beide getrennt an und fragen vor dem Versand über den `ConfirmDialog` (statt `window.confirm`) zurück. **a11y-Fix Tier 3 (Review K-2/K-3):** deren Ergebnis-Zeile trägt jetzt `role="status"`, die Fehler-Zeile `role="alert"` — sonst bekam ein Screenreader-Nutzer bei dieser folgenreichen, törn-einmaligen Aktion keine Ansage. Analog K-1: die DSGVO-Fehlermeldung in `app/profile/delete-account-block.tsx` trägt jetzt `role="alert"`. K-4: die Icon-Buttons in `pending-transactions.tsx` (Entwurf bearbeiten/verwerfen) UND in der Buchungsliste (`transactions-list.tsx` bearbeiten/Details, `delete-button.tsx` löschen) sind jetzt volle 44×44-Touch-Targets (`min-h-touch min-w-touch`) statt ~28 px — die Listen-Stifte fielen beim Browser-Verify neben dem eigentlichen K-4-Fund auf und wurden mitgezogen. **Im Browser verifiziert** (preview_inspect: alle 20 Listen-Controls + Entwurf-Karte = 44×44; role="alert"/"status" an der DSGVO- und Settlement-Meldung live geprüft). Offen/bewusst gelassen: drei 40×40-Header-Nav-Icons (geteiltes Layout, separat zu entscheiden).
- **Settlement-Update-Mail:** Buchungs-Änderungen nach dem initialen Versand (create/update/delete von Expense/Credit) setzen den Marker `trips.changes_pending_since` (über `mark_post_settlement_change()`, gerufen in `lib/actions/transactions.ts`). Ist der Marker gesetzt, sieht **jedes Crewmitglied** auf Trip-Übersicht + Schulden-Seite einen gelben „Bilanz hat sich seit der Abrechnung geändert"-Banner mit Button „Update-Mail verschicken" — typischerweise löst die Person, die soeben die nachträgliche Buchung erfasst hat, die Mail direkt selbst aus. `resendSettlement` in `lib/actions/settlement.ts` schickt das gleiche Mail-Template mit `isUpdate=true` (Subject „Bordkasse-Update", Wortlaut „Bilanz aktualisiert"), aggregiert eine kurze ChangeSummary aus dem Audit-Log (z.B. „3 neu, 1 geändert"), setzt `last_settlement_resend_at` und löscht den Marker. Spam-Schutz: Resend funktioniert nur bei gesetztem Marker — nach jedem erfolgreichen Versand ist der Banner weg.
- **Trip-Datum-Edit:** Skipper/Co-Skipper/Admin können in den Trip-Settings unter „Törndatum" Start- und End-Datum nachträglich ändern (`updateTripDates` in `lib/actions/trips.ts`). Buchungen werden nicht auto-verschoben.
- **Statistik** pro Trip: Live-Aggregation nach Kategorie + Tag. Pill-Toggle „Pro Törn / Pro Person" (Client-Component `stats-view.tsx`) teilt im Pro-Person-Modus alle Geldbeträge durch die aktuelle Crew-Größe (Mengen wie Buchungs-/Tagezahl bleiben). Bleibt nach Purge anonymisiert in `trip_statistics`; Pro-Person-Toggle ist dann mangels Crew-Daten deaktiviert.
- **Gesamt-Statistik** unter `/stats`: aggregiert alle Trips eines Users (Admin: alle Trips im System) — Summary, Kategorie-Bars, Liste nach Törn (mit Drilldown), Bars nach Jahr. Datenquelle gemischt: aktive Trips live aus `transactions`, gepurgte aus `trip_statistics`. Migration 0020 fügt eine schmale `trip_statistics_audience(person_id, trip_id)`-Tabelle hinzu, die beim Purge mit den damaligen Member-Person-IDs (nur mit `auth_user_id`) befüllt wird — RLS-Policies auf `trips` + `trip_statistics` sind um den Audience-Pfad erweitert, damit reguläre User auch nach Purge ihre Cross-Trip-Daten sehen. Query: `lib/queries/global-stats.ts:getGlobalStats`. Einstieg via Link am Seitenende der Trip-Auswahl.
- **Self-Service-Kontolöschung** (DSGVO Art. 17): `/profile` → Block „Konto löschen". Migration 0021 liefert `delete_my_account()` (SECURITY DEFINER, GRANT EXECUTE TO authenticated): anonymisiert `persons` (display_name → „Ehemaliges Crewmitglied", auth_user_id NULL), löscht `persons_private`, `trip_statistics_audience` und Pre-Trip-`trip_members` ohne Buchungs-Spur. Action `deleteMyAccount` in `app/profile/actions.ts` ruft die Function via `admin.rpc` und löscht zusätzlich die `auth.users`-Row. **Blocker:** Buchungen (paid_by / credit_from / credit_to / transaction_participants) in einem aktiven Trip (`end_date >= today`, nicht gepurged) → Action returnt `has_active_bookings`, der User muss bis nach Trip-Ende warten oder den Skipper bitten, seine Buchungen umzubuchen. Nach erfolgreichem Löschen Redirect auf `/?account_deleted=1` mit kleinem Bestätigungs-Banner.
- **Self-Service-Datenexport** (DSGVO Art. 20): `/profile` → Block „Meine Daten exportieren". `exportMyData` in `app/profile/actions.ts` sammelt streng auf die eigene `person.id` begrenzt Profil + Mitgliedschaften + Buchungen/Gutschriften/Einzelanteile/Anzahlungs-Soll und gibt JSON zurück; Client-Component `data-export-block.tsx` bietet es als Blob-Download an.
- **PWA:** Service Worker (`public/sw.js`, `CACHE_VERSION = bordkasse-v13` — bei jedem Deploy hochzählen) cached App-Shell + eine markenkonforme, **iOS-bewusste** `/offline.html` (Fallback für offline nie besuchte Routen; zeigt nicht-installierten iPhones einen „Zum Home-Bildschirm"-Tipp und setzt das `bordkasse:offline-miss`-Flag für die Selbsthilfe). **Kontrollierter Update-Flow:** der SW ruft NICHT mehr unbedingt `skipWaiting()` — eine neue Version bleibt im `waiting`-Zustand, bis der Nutzer im „Neue Version verfügbar"-Banner (`service-worker-register.tsx`; `hadController`-Guard verhindert den Reload beim Erst-Install) auf „Aktualisieren" tippt (→ `SKIP_WAITING`-Message), sonst gäbe es einen erzwungenen Reload mitten in der Eingabe. `manifest.json` bietet `shortcuts` (Meine Törns, Gesamtstatistik). **App-Icons nach Zweck getrennt:** `icon-{192,512}.png` = volles Logo für `purpose:"any"` (Splash, Browser-Tab — kein Crop); `icon-{192,512}-maskable.png` = dasselbe Logo herunterskaliert in die Android-Safe-Zone (zentr. Kreis r=40 %) auf `#FAFBFC`, `purpose:"maskable"` — sonst schnitt Android (adaptive Kreis-/Squircle-Maske) die Segelspitzen ab (Splash + Startbildschirm-Icon). Maskable-Varianten reproduzierbar via `python3 scripts/make-maskable-icons.py` (PIL; bei Logo-Änderung neu erzeugen). `apple-icon.png` bleibt vollflächig (iOS rundet nur die Ecken auf weißem Grund → kein Eingriff nötig). IndexedDB-Outbox erfasst Buchungen offline und synchronisiert beim Reconnect. Der `OfflineBanner` ist im **Trip-Layout** gemountet (nicht global → Outbox-/IndexedDB-Code bleibt aus dem JS-Bundle der öffentlichen Seiten heraus); `ServiceWorkerRegister` bleibt global, damit der SW auch für ausgeloggte Besucher registriert ist. Der Banner zeigt dauerhaft fehlschlagende Sync-Einträge mit „Erneut"/„Verwerfen" (statt eines ewig hängenden Zählers); Buchungsformulare retten zudem einen **Mid-Flight-Netzdrop** (offline NACH dem Klick) dedup-sicher in die Outbox. **iOS-Härtung (Fix Offline-Buchen):** `storage.persist()` wird beim Start angefragt (best effort; auf iOS-Safari-Tabs meist wirkungslos → echte Abhilfe ist die Installation). Findet der FAB offline das Buchungsformular NICHT im Cache (`isFormCached` über alle `…-pages`-Caches, `ignoreSearch`/`ignoreVary`, fail-open bei fehlendem Cache-API), läuft er nicht mehr in die `/offline.html`-Sackgasse, sondern zeigt einen Toast + setzt das `offline-miss`-Flag; beim nächsten Online-Sein blendet der `OfflineBanner` eine kurze Selbsthilfe ein (clear-on-show). Ein iOS-In-App-Browser (Outlook/Gmail — oft kein SW, getrennter Speicher) wird via `isInAppBrowser()` (`lib/pwa.ts`, neben `supportsOffline()`) erkannt und auf der Törn-Übersicht durch die `InAppBrowserWarning` („in Safari öffnen / installieren", Link-kopieren) adressiert; daneben ein törn-eigener iOS-Install-Nudge (`InstallHint` mit eigenem `dismissKey`, gated über die geteilte PWA-Erkennung). Cache-Preflight + Flag-Helper: `lib/offline/offline-help.ts` (Vitest: `__tests__/pwa.test.ts`, `__tests__/offline-help.test.ts`). **Code-Review-Härtung Runde 2 (Tier D, Offline/FX):** (O-1) `syncOutbox` hält pro Eintrag ein `isSyncing`-Lock und liest den Eintrag FRISCH vor dem Replay (statt aus dem `listAll`-Snapshot); der Draft-Editor bricht das Speichern mit `?toast=draft-syncing` ab, wenn der Eintrag gerade synchronisiert wird — sonst gewann bei einer Bearbeitung während des Syncs der Server-Insert mit gleichem `idempotency_key` und die Änderung ging still verloren (`__tests__/sync.test.ts`). (O-2) `networkFirst` cacht **keine gefolgten Redirects** mehr (`!response.redirected`): eine abgelaufene Session hätte sonst die Login-Seite unter dem Buchungsformular-Key abgelegt → Offline-Sackgasse trotz `isFormCached`-Preflight. (O-4) `rscNetworkFirst` awaitet `cache.put` nicht mehr (fire-and-forget mit `.catch`) — ein `QuotaExceededError` machte sonst eine bereits erfolgreiche Online-Navigation kaputt. (O-5) der `useBookingSubmit`-Hook rettet eine neue Buchung auch bei **Netzwerk-Rejection der Server-Action** in die Outbox (`createWithRescue`-Wrapper), nicht nur beim `offline`-Event — deckt totes Uplink-WLAN ab, wo `navigator.onLine` true bleibt; dedup-sicher via `idempotency_key`. (O-3/C-2 Fremdwährung) der Draft-Editor stellt `bank_foreign_amount` wieder her (nur aus dem Outbox-Entwurf möglich, dort in `formData`), damit der Replay den effektiven Kurs mit dem RICHTIGEN Divisor rechnet; die **Edit-Seite** einer bank-bestätigten Fremdwährungsbuchung belegt das Bank-Feld NICHT mehr mit `tx.amount` vor (das war der geteilte EUR-Betrag, nicht der abgebuchte) — der gespeicherte `exchange_rate` ist bei `rate_source='bank'` bereits der effektive Kurs und repreist beim Ändern des Fremdbetrags korrekt. (O-6, Tier 2) die „wartet"-Karten in `pending-transactions.tsx` zeigen Fremdwährungs-Entwürfe jetzt in der Fremdwährung (`500,00 SEK`) statt fälschlich als `500,00 €` — `describe()` liest `original_currency` aus der Outbox-`formData`.
- **Push-Benachrichtigungen** (Web-Push, opt-in pro Gerät, Migration 0038 = `push_subscriptions`): zusätzlich zur Mail — **nie als Ersatz** (Skipper-Entscheid: Push wird weggewischt, die Mail liefert Kontext). Aktivierung im `/profile`-Block (`notification-block.tsx`, Permission nur auf Klick, iOS nur als installierte PWA, `urlBase64ToUint8Array` für `applicationServerKey`); Subscribe/Unsubscribe-Actions in `push-actions.ts` (Upsert auf `endpoint`). Geteilter Hook `use-push-subscription.ts` trägt zusätzlich einen wegklickbaren **Übersicht-Nudge** (`notification-nudge.tsx`, nur bei `status==="unsubscribed"`, localStorage-Dismiss), damit die Funktion nicht in den Profil-Settings verschimmelt. Monochromes Android-Badge `/badge-96.png` (`scripts/make-badge.mjs`). Dispatch in `lib/notify/web-push.ts` (`sendPushToPersons`, **wirft nie** wie `sendMail`, löscht Abo bei HTTP 404/410), reine Payloads/Empfänger in `lib/notify/{payloads,recipients}.ts` (Vitest). SW (`bordkasse-v9`) hat `push`- + `notificationclick`-Listener mit **Foreground-Suppression** (sichtbares Fenster auf gleichem Trip → keine Doppel-Mitteilung; anderer Trip wird NICHT verschluckt) + `tag`-Collapse. Verdrahtet **additiv nach** der jeweiligen Mail-Schleife an: Settlement announce/resend, Schuld-abgehakt (nur Gegenpartei, **kein Observer**), Anzahlungs-Reminder-Cron (crew + advancer, teilt `prepayment_reminder_log` mit der Mail), Selbstmeldung/Bestätigung/Ablehnung/Erfassung. Auslöser jeweils via `pushRecipients({ excludeActorId })` ausgenommen. **DSGVO:** `delete_my_account()` löscht die Abos **explizit** (persons-Row wird nur anonymisiert → `ON DELETE CASCADE` feuert nicht), `search_path`-Pin aus 0033 in 0038 inline neu gesetzt. Env: `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT`. Spec: `docs/push-notifications.md`. Bewusster Scope: manueller Einzel-Reminder (🔔) bleibt Mail-only; echter Geräte-Push ist nur manuell testbar (headless nicht herstellbar). **Härtung (Review-Fixes):** `endpoint` https-only + Push-Host-Allowlist in `savePushSubscription` (SSRF-Schutz, Test `__tests__/push-actions.test.ts`); `disable()` kündigt das Browser-Abo nur bei eigener DB-Zeile (kein Orphan auf geteilten Geräten); geteilte PWA-Erkennung `lib/pwa.ts` (iPadOS-Desktop-UA); Settlement-Payloads `alwaysShow` (Suppression aus, da `RealtimeTrip` `trips` nicht abonniert); `notificationclick` fokussiert das passende Fenster statt des ersten.
  - **Offline-Entwürfe bearbeiten/löschen:** Noch nicht gesyncte Outbox-Buchungen erscheinen als „wartet"-Karten oben in der Buchungsliste (`pending-transactions.tsx`, client-seitig aus IndexedDB, `useSyncExternalStore` für Online-Status + `subscribeToChanges`). Bearbeiten öffnet das Form im **Draft-Modus** (`/trips/[id]/transactions/new?draft=<outbox-id>` → `draft-editor.tsx` lädt das Item, `draftToExpenseInitial`/`draftToCreditInitial` befüllen die bestehende `initial`-Maschinerie vor; `transaction-form.tsx` schreibt bei gesetztem `draftId` per `enqueue` mit **gleicher id** zurück in die Outbox statt Server-Call → stabiler `idempotency_key`, kein Duplikat). **Online-Race-Schutz:** Draft-Submit prüft erst `get(draftId)`; ist der Eintrag schon weggesynct, Hinweis-Toast `draft-synced` statt stillem No-Op. Löschen = `remove(id)` mit `useConfirm`. Outbox-Helper `get(id)` in `lib/offline/outbox.ts`. Edit/Delete von bereits **gesyncten** Server-Buchungen bleiben online-only (unverändert).
  - **Form-Preload für See-Einsatz:** `PrefetchOfflineForm` (im Trip-Layout) wärmt beim Online-Öffnen eines Törns — **und erneut bei `online`/`visibilitychange`, ohne `setTimeout`-Rennen** (verließe der Nutzer den Törn zu früh, blieb das Formular sonst ungewärmt) — `GET /trips/<id>/transactions/new` mit `Accept: text/html` → der SW legt das voll gerenderte Form-Dokument (inkl. aktueller Crew) per `networkFirst` ins PAGES_CACHE. Offline macht der FAB (`bottom-nav.tsx`) zuerst einen **Cache-Preflight** (`isFormCached`); nur bei Treffer folgt die **Hard-Navigation** (`window.location.assign` statt Client-RSC-Nav, die der SW nicht cacht) → der SW liefert das gewärmte Dokument. Kein Treffer → Toast + `offline-miss`-Flag statt `/offline.html`-Sackgasse (fail-open, wenn das Cache-API ganz fehlt, z. B. In-App-Browser). Der Entwurf-„Bearbeiten"-Link erzwingt ebenfalls eine Hard-Navigation. `networkFirst` fällt bei Cache-Miss auf `ignoreSearch`-Match zurück, damit `…/new?draft=X` auf das gewärmte `…/new` trifft.
- **Törn-Fortschritt-Checkliste** (Migration 0029 = `trip_members.checklist_collapsed_at`; reine Logik `lib/calc/trip-progress.ts` → `computeTripProgress`, Vitest-getestet): Karte „Dein Törn im Überblick" auf der Trip-Übersicht (**unter** den Schnellzugriff-Kacheln), sichtbar nur für Skipper/Co-Skipper/Admin (wie der Settlement-Banner). Bildet den gesamten Törn-Lebenszyklus in **5 Phasen** ab: Vorbereitung → Anzahlung (nur Charter) → Während des Törns → Abrechnung → Abschluss. **Fortschritts-Spiegel statt Todo-Liste:** fast alle Haken werden aus dem echten Datenstand abgeleitet (`lib/queries/trip-progress.ts` reused `getPlan`/`getPrepaymentPoolBalances`/`getCharterPaidTotal` + `getSimplifiedDebts`/`getSettledDebtKeys`; `all_debts_settled` in TS statt service-role-RPC). **Einzige Ausnahme — „Kaution verrechnet" ist manuell** (Migration 0037 = `trips.deposit_settled_at`): das Item trägt `manual: true` (kein `href`), die UI rendert eine echte Checkbox (`role=checkbox`, optimistisch mit Reset bei Fehler), Toggle über `setDepositSettled` in `lib/actions/trip-checklist.ts` (Skipper/Co-Skipper/Admin via `requireSkipperOrAdmin`, Trip-Level-Fakt). Bewusst manuell statt der alten Namens-Heuristik (`/kaution/i`-Kategorie + nicht-gelöschte Buchung), die zu viele Eventualitäten verfehlte (umbenannte Kategorie, Gegenverrechnung per Gutschrift, gar keine eigene Buchung). Aktuelle Phase auto-aufgeklappt (erste nicht-erledigte), Items zukünftiger Phasen „noch nicht dran" (nie „offen/rot"), bei allem erledigt kompakte Abschluss-Zeile. Pro Mitglied minimierbar (`trip_members.checklist_collapsed_at`, DB-persistiert → geräteübergreifend; Server-Action `lib/actions/trip-checklist.ts`). a11y: `role=progressbar`, Status als Icon **und** Text (nicht nur Farbe/Emoji), Touch-Targets. **Anzahlungs-Dreizustand (Migration 0040 = `trips.prepayment_declined_at`):** „Charter" = es existiert ein `prepayment_plan` (gewinnt immer; `savePrepaymentPlan` löscht das Declined-Flag). Zusätzlich wählt der Skipper bei der **Törn-Anlage** (Radio „Mit Anzahlungsplan"/„Ohne Anzahlung" in `new-trip-form.tsx`, Default „mit") bzw. später in den **Settings** (Sektion „Anzahlungsplan", `setPrepaymentDeclined` in `lib/actions/trips.ts`), ob eine Anzahlung vorgesehen ist. Nicht abgewählt + kein Plan → Vorbereitung-Item „Anzahlungsplan anlegen" (offen, → Wizard; erledigt sobald Plan existiert) + der kontextuelle CTA `showCreatePrepaymentCta` auf der Übersicht. Abgewählt (declined) → weder Item noch CTA; die Karte „ergibt Sinn" ohne ewig nagende Anzahlungs-Reste. Die Anzahlungs-Phase (einsammeln) erscheint weiterhin erst mit angelegtem Plan. (Historie: das frühere Flag `has_charter_prepayment` wurde in 0030 entfernt, weil ohne gespeicherte ENTSCHEIDUNG „Plan existiert" die einzige Wahrheit war — mit der expliziten Wahl ist das Flag jetzt sinnvoll.) „Alle Schulden beglichen" zählt erst nach Abrechnungs-Versand als erledigt (sonst zeigt ein Törn ohne Buchungen den Haken verfrüht). Verlinkt den Settlement-Banner, statt ihn zu duplizieren.
- **Audit-Log:** jede Schreib-Operation hinterlässt einen Eintrag, RLS-Lese-Schutz auf Skipper.
- **Soft-Delete:** Buchungen tragen `deleted_at` statt physisch gelöscht zu werden.
- **DSGVO-Datenlöschung:** täglicher Vercel-Cron `/api/cron/purge` → `purge_expired_trip_data()`. Purge nur wenn end_date + 30 Tage in Vergangenheit UND `settlement_announced_at` gesetzt UND alle `simplify_debts` in `settled_debts` abgehakt (Helper `all_debts_settled`). Single-Trip-Variante `purge_trip_data(trip_id, force)` für den manuellen Skipper-/Admin-Button in den Trip-Settings; Force überspringt Retention + Settlement, NICHT Schulden. Trip-Auswahl-Seite markiert überfällige Trips rot + Banner für Skipper/Admin (`retention_overdue`-Flag in `listMyTrips`). Aggregierte Statistik bleibt in `trip_statistics`. **Härtung Migration 0042:** (a) `purge_trip_data` löscht jetzt explizit die Anzahlungs-Tabellen (`prepayment_reminder_log`, `prepayment_obligations`, `prepayment_tranches`, `cabin_types`, `prepayment_plan` inkl. `wero_id`) — deren `trip_id`-CASCADE feuerte nie, weil die `trips`-Row nur anonymisiert statt gelöscht wird (DSGVO-Leftover, Review-Fund Q2). (b) `settlement_announced_by` wird beim Purge mit-genullt und die Orphan-Personen-Bereinigung prüft jetzt ALLE NO-ACTION-FK-Spalten (`trips.skipper_id`/`settlement_announced_by`, `transactions.paid_by`/`credit_from`/`credit_to`/`created_by`, `transaction_participants.person_id`) statt nur `trip_members` — sonst konnte eine anonymisierte Ex-Person, die in einem ANDEREN Törn referenziert war, eine FK-Violation auslösen (Q3a). (c) `purge_expired_trip_data` kapselt jeden Törn in einen eigenen `BEGIN … EXCEPTION`-Subtransaktionsblock: ein fehlerhafter Törn wird geloggt (`RAISE WARNING`) und übersprungen, statt den ganzen Cron-Lauf zurückzurollen (Q3b). `delete_my_account` hatte das GLEICHE Leftover-Problem für `prepayment_obligations`/`prepayment_reminder_log` (Review-Fund Q5) — **gefixt in Migration 0043** (die vorherige gegenteilige Notiz hier war veraltet). **Buchungs-Kerndaten bleiben jetzt erhalten (Migration 0044):** `purge_trip_data` löscht `transactions`-Zeilen nicht mehr, sondern nullt nur die personenbeziehbaren Spalten (`paid_by`/`credit_from`/`credit_to`/`created_by`) — Betrag, Beschreibung, Alkohol-/Trinkgeld-Anteil, Datum, Kategorie und Aufteilungsart bleiben als Vergleichs-/Erfahrungswert für künftige Törns erhalten. `transaction_participants` (wer war beteiligt, wie aufgeteilt) wird weiterhin komplett gelöscht. `trip_categories` werden dafür NICHT mehr gelöscht (keine Personendaten, sonst würde `transactions.category_id` verwaisen). **CHECK-Constraints gelockert (kritischer Fund aus lokaler pgTAP-Verifikation vor dem ersten Produktions-Push):** `tx_expense_fields` verlangte bisher `paid_by IS NOT NULL` für jede Ausgabe, `tx_credit_fields` `credit_from IS NOT NULL` für jede Gutschrift — die anonymisierende UPDATE oben hätte damit für JEDEN echten Törn mit Ausgaben/Gutschriften mit einer Constraint-Violation abgebrochen (nur mit einem leeren Test-Törn ohne solche Zeilen wäre das unbemerkt geblieben). `tx_expense_fields` verlangt jetzt nur noch `split_type IS NOT NULL` (unberührt vom Purge), `tx_credit_fields` wurde ersatzlos entfernt — die Pflichtfelder bleiben bei der Neuanlage weiterhin über `ExpenseSchema`/`CreditSchema` (Zod) erzwungen. Neue Audience-RLS-Policies `tx_select_audience`/`tc_select_audience` (gleiches Muster wie `trip_statistics_audience` aus 0020) lassen ehemalige Crewmitglieder mit Login die anonymisierten Buchungen weiterhin lesen. **`credit_to_all`-Flag (Migration 0046, Fund aus unabhängiger Review):** die App-Logik nutzte bisher `credit_to IS NULL` als alleinigen Bedeutungsträger für „An Alle" (Anzeige in Buchungsliste/-detail). Da der Purge JEDE Gutschrift nullt (auch direkte), hätte eine anonymisierte direkte Gutschrift danach fälschlich als „An Alle" angezeigt — das ist keine Anonymisierung mehr, sondern eine inhaltliche Verfälschung. `transactions.credit_to_all` (bei Erstellung gesetzt, vom Purge unangetastet) trägt diese Unterscheidung jetzt unabhängig vom Personenbezug weiter; nicht mehr aufgelöste, nicht-„An Alle"-Empfänger zeigen „Empfänger nicht mehr verfügbar". Regressionstests: `supabase/tests/purge_keep_transaction_facts_test.sql` (inkl. negativem RLS-Check für einen eingeloggten Fremden ohne Audience-Spur). **Bugfix (gleiche PR):** der manuelle Löschen-Button (`purgeTripNow` in `lib/actions/trips.ts`) prüfte auf die falschen String-Werte (`retention_not_reached`/`settlement_not_announced`) statt der tatsächlichen SQL-Rückgabewerte (`too_young`/`no_settlement`) — dadurch zeigte JEDER Klick vor Fristablauf/Abrechnungsversand fälschlich „Unerwartete Antwort der Datenbank" statt der freundlichen Erklärung.
- **Public Pages (kein Login nötig):** `/`, `/login`, `/datenschutz`, `/kontakt`, `/about` — in `proxy.ts` (in Next 16.2 von `middleware.ts` umbenannt; Funktion heißt jetzt `proxy`) als `PUBLIC_ROUTES` eingetragen. Statische `public/`-Dateien (`robots.txt`, `manifest.json`, `sw.js`, `offline.html`) sind zusätzlich aus dem `config.matcher` ausgenommen — sonst würden sie für nicht-eingeloggte Nutzer auth-gegated (307 → /login), was Crawler-`robots.txt` + PWA-Manifest/Service-Worker auf den öffentlichen Seiten bräche (Fix #151). **`/kontakt` (bewusst KEIN formales Impressum):** Die App ist privat, nicht-kommerziell und invite-only → die Impressumspflicht (§ 5 DDG / § 18 MStV) greift aller Voraussicht nach nicht (Ausnahme „ausschließlich persönlich/familiär"). Deshalb nur eine schlanke Kontakt-Seite (Name + Mail, **keine** ladungsfähige Wohnanschrift → kein Doxxing); Verantwortliche bleiben über die Datenschutzerklärung identifizierbar. Wird die App je kommerziell (Werbung/Gebühren/Verkauf), wird ein vollständiges Impressum Pflicht. `/about` ist die Funktionsübersicht der App mit **19 Mobile-Screenshots** aus echtem Betrieb (synthetische Crew, keine echten Personendaten) — inklusive drei Anzahlungs-Karten (Setup-Wizard, Matrix mit Charter-Banner, Crew-Self-View), der Törn-Fortschritt-Karte (`18-toern-fortschritt.webp`) und der Fremdwährungs-Buchung (`19-fremdwaehrung.webp`, Feature „In Fremdwährung buchen" in der Phase „Während des Törns"). Weil der Demo-Ostseetörn dafür Fremdwährungen aktiviert hat, zeigen `06-buchung-neu` + `12-gutschrift` jetzt zusätzlich den (auf Euro stehenden) Währungswähler. Jeder Screenshot wird über die Client-Component [`webapp/app/about/feature-shot.tsx`](webapp/app/about/feature-shot.tsx) **vollständig (kein Crop) in einem flach-minimalen Phone-Frame** dargestellt (dünne Bezels, große Eckenradien, on-brand dunkler Rahmen `bg-ink`, bewusst KEINE Notch — würde Inhalt verdecken und schnell veraltet wirken); Tap öffnet eine Vollbild-Lightbox zum Reinzoomen. Die Features sind in einem **Phasen-Explorer** [`webapp/app/about/about-explorer.tsx`](webapp/app/about/about-explorer.tsx) organisiert (Client-Component, bekommt die Phasen-Daten inkl. vorgerendeter Icons + JSX-`body` vom Server-`page.tsx`): eine **sticky ARIA-Tab-Leiste** schaltet zwischen den 5 Törn-Phasen um (mobil horizontal scrollbar, Pfeil-/Home-/End-Tastatursteuerung, roving tabindex), nur die aktive Phase ist sichtbar. Pro Feature ein **alternierendes Zwei-Spalten-Layout** ab `md` (Phone mal links, mal rechts, Text daneben), auf Mobile gestapelt. Über der Tab-Leiste sitzt ein **Rollen-Filter** (`Alle · Nur Skipper · Nur Crew`), der rollenspezifische Funktionen ein-/ausblendet (Tab-Zähler werden mitgefiltert, leere Phasen ausgegraut, Tastatur-Nav überspringt sie; ein Feature-Deep-Link setzt den Filter auf `Alle`); jedes Feature trägt ein **Rollen-Badge** (Skipper → `Anchor`, Crew/Alle → `Users`; **lucide-Strich-Icons wie im Rest der App, keine Emoji** — Icon + Text, nicht nur Farbe; `roleFor()` in `page.tsx`). Über den Tabs steht ein neutraler **Rahmungs-Einzeiler** (`intro`-Prop) zur 5-Phasen-Logik — bewusst KEIN rollenspezifisches Feature als Hero; die Törn-Fortschritt-Karte selbst sitzt als Skipper-Feature in der Phase „Loslegen" (neben „Startseite des Törns"). Desktop: Container `max-w-5xl`; **alles teilt eine gemeinsame linke Kante** — Hero/Lead/„nicht ist"-Box sind `max-w-3xl` **linksbündig** (NICHT `mx-auto` — das erzeugte sonst einen Versatz zur Tab-Leiste), brechen also nur früher um, während der Explorer die volle Breite nutzt; Phone-Frames sind auf 248px gedeckelt. Nur der Abschluss-CTA + Footer sind bewusst seitenmittig. „Mehr Details" ist progressive disclosure NUR auf Mobile (`<details>` eingeklappt) — auf Desktop ist der Erklärtext direkt neben dem Phone offen. Deep-Links: `#<phaseId>` öffnet die Phase, `#<featureId>` öffnet die passende Phase und scrollt zum Feature (Live-`hashchange`). Die Screenshots liegen als **WebP** (~65 % kleiner als PNG, von `page.tsx` referenziert); das erste Bild der aktiven Phase lädt `eager`/`fetchPriority=high` (LCP), die Nachbarphasen werden in der Idle-Zeit vorgeladen. Screenshots reproduzierbar via `webapp/scripts/take-screenshots.ts` (Playwright nimmt PNG-Buffer auf → `sharp` schreibt WebP raus; + Mailpit-Magic-Link + lokales Supabase). Komplettes Demo-Setup über das Helper-Skript [`webapp/scripts/seed-demo.sh`](webapp/scripts/seed-demo.sh) — es macht `supabase db reset`, fängt das kaputte `seed.sql` ab, legt Auth-User für Anna + Clara via Admin-API an (direkte `auth.users`-INSERTs sind in der aktuellen Supabase-Version unzuverlässig — „Database error finding user"), spielt `seed_demo.sql` ein und verknüpft `persons.auth_user_id`. Mit `--screenshots` läuft im Anschluss die Playwright-Pipeline. Demo enthält **drei Trips**, deren Zeiträume + Namens-Jahre **dynamisch aus dem Tagesdatum** (`CURRENT_DATE`) berechnet werden (driftfrei bei jedem Screenshot-Lauf — siehe Kopf-Kommentar in `seed_demo.sql`): „Ostseetörn {laufendes Jahr}" (aktueller, **laufender** Törn — heute = Tag-Mitte, daher bewusst KEIN Settlement-Banner; trägt die Buchungs-/Bilanz-/Schulden-/Statistik-Demo), „Kroatien {Vorjahr}" (vergangener Törn im September, vollständig abgerechnet & beglichen) und „Korsika {Folgejahr}" (zukünftiger Charter mit Anzahlungs-Plan: Kojen, Vorstrecker Anna, 2 Tranchen, gemischte Zahlungs-Stati inkl. einer Pending-Selbstmeldung; Tranche 1 fällig heute+10 → „bald fällig"). Clara hat einen eigenen Auth-User für den Crew-Self-View-Screenshot (Re-Login mid-pipeline).
  - **Screenshot-Runbook (WICHTIG, gegen Mac-Überlast):** Screenshots IMMER gegen den **Production-Server** (`pnpm build` → `pnpm start`) aufnehmen, NICHT gegen `pnpm dev`. Turbopack-Dev kompiliert Routen lazy → Playwright läuft in 30s-`load`-Timeouts und unter gleichzeitiger colima/supabase/dev-Last kann der Mac einfrieren. Bewährte Reihenfolge: (1) `colima start`, (2) `supabase start` + blockieren bis API 200, (3) `./scripts/seed-demo.sh`, (4) `pnpm build` — Schritte 1–4 als je eigenes abgeschlossenes Kommando; (5) `pnpm start &` + Routen vorwärmen (`curl / /login /about`) + `take-screenshots.ts` + `kill` in EINEM Kommando. Kein Background-Polling über Turns, kein `|| echo`, kein Retry-Loop. Vollständiges Runbook im Kopf-Kommentar von `webapp/scripts/take-screenshots.ts`. Code-Push immer von Screenshots entkoppeln.
- **Hosting-Region:** Vercel `regions: ["fra1"]` in `vercel.json` (sonst US-Default `iad1`); Mailserver whost.dev (DE).
- **Hosting-Stand (seit 2026-08-05):** Die **Datenbank läuft selbst gehostet** — Supabase-Slim-Stack per Docker Compose unter Coolify auf einem Hetzner-Server in Deutschland (`webapp/supabase/self-host/`, Runbook `docs/self-hosting.md`). Auslöser war der Supabase-Free-Tier, der Projekte nach ~7 Tagen Inaktivität pausiert. Supabase Cloud (Central EU) existiert noch als Rückweg und wird erst gelöscht, wenn ein echter Törn auf dem neuen Stack gelaufen ist. **Die App läuft weiterhin auf Vercel** und zeigt per Env-Vars auf den neuen Stack; der Container für den App-Umzug liegt fertig im Repo (`webapp/Dockerfile` + `output: "standalone"`), umgeschaltet wird erst nach dem nächsten Törn. Zwei Fallen fürs Umschalten: (a) `vercel.json` erst DANN entfernen — es trägt neben den zwei Crons auch die `fra1`-Region; (b) in Coolify müssen ALLE `NEXT_PUBLIC_*`-Variablen **Build Variables** sein, weil Next sie beim Build einbackt (auch in Server-Code) — sonst u. a. kaputte Magic-Links. Beim Umschalten fällt außerdem die Datenschutzerklärung an (Vercel Inc. + Supabase Inc. raus, Server-Betreiber + Hetzner rein).
- **RAM-Optimierung (05.08.2026, gemessen):** Der Stack braucht **375 MB** statt vorher 1.207 MB. Zwei Eingriffe: (a) `KONG_NGINX_MAIN_WORKER_PROCESSES: 1` — Kong startete sonst einen nginx-Worker pro CPU-Kern (acht auf dem Server) und zog **614 MB**, mehr als der ganze Rest zusammen; jetzt 81 MB. ⚠️ Der Präfix ist die Falle: `KONG_NGINX_WORKER_PROCESSES` (ohne `MAIN`) wird von Kong **stillschweigend ignoriert**, die generierte nginx.conf sagt weiter `worker_processes auto`. (b) `studio` + `meta` (282 MB) liegen im Compose-Profil `studio` und starten nicht mehr mit — bei Bedarf `docker compose --profile studio up -d studio`; solange sie fehlen, fragt Kong auf `/` das Basic-Auth ab (401) und liefert erst nach dem Login 502. Dafür musste `depends_on: studio` bei Kong weg (Compose bricht ab, wenn ein aktiver Service von einem inaktiven Profil-Service abhängt). Postgres bleibt unangetastet: 74 MB bei 78 KB Dump-Größe — dort ist nichts zu holen. Realtime (201 MB) bleibt, sein Wegfall wäre echter Funktionsverlust.
- **Backup (seit dem Umzug Eigenverantwortung):** `webapp/supabase/self-host/backup.sh` läuft **im `db`-Container** (Coolify Scheduled Task `bordkasse-backup`, täglich **00:00**, Timeout 300 s) — nicht per `docker exec` vom Host, weil Coolify-Tasks im Container laufen. Dumpt als `supabase_admin`, NICHT als `postgres`: sonst fehlen die `auth`-Tabellen **still** (sie gehören `supabase_auth_admin`) und man hätte nach dem Restore die Zahlen, aber keine Logins. Prüft Integrität + Inhalt, 30 Tage im Named Volume `db-backups`. **Auslagern:** der Server-Betreiber zieht `db-backups` um **02:00** vom Server, verschlüsselt (AV mit Hetzner vorhanden, keine weiteren Cloud-Dienste). Die beiden Uhrzeiten sind abgestimmt — 00:00 dumpen, 02:00 wegholen; wer die Dump-Zeit verschiebt, muss das mit ihm klären, sonst wandert nachts der Vortagsstand nach außen. `db-data` (PGDATA) wird bewusst NICHT gesichert (Dateisystem-Kopie einer laufenden Postgres-Instanz ist inkonsistent).
- **Security:** RLS-Policies, Service-Role nur in Server Actions, Security-Header (HSTS/CSP/X-Frame/Referrer-Policy), `noindex`-Meta + `robots.txt`. **IDOR-Schutz im App-Layer (wichtig, da Service-Role RLS umgeht):** schreibende/löschende Actions filtern IMMER zusätzlich `trip_id` (nicht nur die Row-ID) — gilt für `deleteTransaction`, Kategorie-Actions und alle Buchungs-/Prepayment-Pfade inkl. `tranche_id`-Zugehörigkeit (`trancheBelongsToTrip`). Zusätzlich prüft `personsBelongToTrip` in `createExpense`/`updateExpense`/`createCredit`/`updateCredit` **und `recordPayment`** (Anzahlung, Fix S-2), dass `paid_by` / `participant_ids` / `credit_from` / `credit_to` / Anzahlungs-`person_id` wirklich Crew dieses Törns sind (Cross-Trip-Personen-Schutz; „Alle"=`null` wird übersprungen). Die Helper `personsBelongToTrip` / `trancheBelongsToTrip` / `CROSS_TRIP_PERSON_MSG` liegen geteilt in `lib/auth/cross-trip.ts` (von `transactions.ts` + `prepayments.ts` genutzt). **Code-Review-Härtung (Runde 2):** (S-1) `replayPendingTransaction` (Offline-Outbox-Replay) durchläuft jetzt DIESELBEN Guards wie `createExpense`/`createCredit` — `checkMinShare`, `checkOnBoardDate`, `personsBelongToTrip` und `markPostSettlementChange` — vorher war der exportierte Replay-Pfad ein guard-freier Seiteneingang (tranche_id wird dort bewusst nicht geparst → keine Pool-Injektion). (S-2) `deleteTransaction` verlangt jetzt `canEditTransaction` (Ersteller/Skipper/Admin) statt bloßer Mitgliedschaft — die destruktive Aktion war schwächer geschützt als das Editieren. (S-3) `recordPayment` prüft `overflow_tranche_id` via `trancheBelongsToTrip` (nicht nur die primäre Tranche). (S-5) der Overflow-Split (`part2`) bekommt einen abgeleiteten `idempotency_key` (`${key}:overflow`) — vorher war er key-los und ein Retry duplizierte den Overflow-Betrag, während `part1` an der Unique-Violation still „gelang". (S-4) `updateExpense` rollt bei fehlgeschlagenem Participants-Delete/Insert Buchungszeile + alte Anteile aus einem Snapshot zurück (kompensierende Aktion, da keine echte Transaktion über den Service-Role-Client) — sonst blieb die Buchung mit neuem Betrag, aber 0 Anteilen zurück (Bilanz ≠ 0). (S-6) `toggleDebtSettled` validiert `(from,to,amount)` gegen den echten `simplify_debts`-Output des Törns und speichert den kanonischen Betrag — vorher konnte ein Mitglied die Gegenseite auf eine beliebige (auch trip-fremde) Person + Fantasiebetrag setzen und ihr eine „Zahlung abgehakt"-Mail/Push schicken. Regressionstests: `__tests__/transaction-actions.test.ts` (Replay-Cross-Trip, Replay-An-Bord, Delete-Authz). **View-RLS (Fix S-1, kritisch):** Migration `0035_view_security_invoker` stellt alle 6 Views (`v_balances`, `v_transaction_shares`, `v_balances_bordkasse_only`, `v_prepayment_payments`, `v_prepayment_pending`, `v_trip_members_with_days`) auf `security_invoker = on` und entzieht `anon` das `SELECT` — vorher liefen Views mit Definer-Rechten (BYPASSRLS) und der `anon`-Key konnte Finanzdaten aller Törns über die REST-API lesen. Crew liest weiter über RLS (Cookie-Client), Admin/Cron/Mail über Service-Role. Regression: `supabase/tests/view_security_invoker_test.sql`. **Ghost-Merge nur ohne Login (Fix S-3):** `updateMember` mergt einen Ghost nur dann automatisch in einen E-Mail-Kollisions-Account, wenn dieser KEINEN `auth_user_id` hat (echter Platzhalter); gehört die E-Mail einem registrierten Konto, blockt die Action und verweist auf „Crew einladen" (Einwilligung statt stillem Absorbieren fremder Identität). **Cron-Auth (Fix S-5):** `lib/auth/cron-auth.ts:verifyCronAuth` vergleicht das `CRON_SECRET` konstant-zeitlich (`timingSafeEqual` + Längen-Guard), genutzt von `purge` + `prepayment-reminders`. **server-only (Fix S-6):** `admin.ts`, `read-client.ts`, `get-current-person.ts` importieren `"server-only"` (versehentlicher Client-Import bricht den Build statt den Service-Role-Key auszuliefern). **DB-Härtung:** Migration `0033_pin_search_path` nagelt `search_path = pg_catalog, public` auf alle SECURITY-DEFINER-Funktionen (`is_trip_member/-skipper`, `current_person_id`, `purge_*`, `delete_my_account`) + `all_debts_settled` fest; `0034_audit_log_rls` ersetzt die `audit_log`-SELECT-Policy durch `trip_id IS NOT NULL AND is_trip_skipper(trip_id)` (entfernt den offenen `trip_id IS NULL`-Lesezweig). **CSP (Fix S-4):** `script-src` lässt in PRODUKTION `'unsafe-eval'` weg (`next.config.ts`, env-abhängig) — der prod-Bundle braucht kein eval, nur Turbopack/HMR im Dev. `'unsafe-inline'` bleibt bewusst (Next App Router gibt auf jeder Seite Inline-RSC-Skripte aus; ein Per-Request-Nonce ginge nur dynamisch und würde die statisch vorgerenderten Public-Seiten brechen). **Magic-Link-Auth:** `resolveOrigin` akzeptiert den `Origin`-Header nur gegen eine Allowlist (sonst Redirect-Poisoning) und nutzt als Env-Origin `NEXT_PUBLIC_SITE_URL` **mit Fallback auf `NEXT_PUBLIC_APP_ORIGIN`** (beide auf der Allowlist) — eine der beiden Variablen genügt; fehlen BEIDE in Production, wird fail-loud geworfen. (Genau diese Lücke — `APP_ORIGIN` gesetzt, `SITE_URL` nicht — ließ Crew-Invites in Prod abstürzen.) `safeNextPath` schützt Post-Login-Redirects vor Open-Redirect. `lib/utils.ts:nowMs()` kapselt `Date.now()` (react-hooks/purity-Regel verbietet impure Calls in Komponenten/Effekten).
- **Error-/Loading-Boundaries:** `app/error.tsx` + `global-error.tsx` + `not-found.tsx` (markenkonform, deutsch) fangen Render-/DB-Fehler statt nackter Next-500/404; `getTrip` wirft bei echtem DB-Fehler (→ Error-Boundary) und liefert nur bei 0-Rows `null` (→ `notFound()`). Lade-Skeletons (einheitlich zentriert via `<main class="mx-auto max-w-2xl …">`, kein Layout-Sprung beim Tab-Wechsel) in `trips/[id]/{,transactions,balance,debts,stats}/loading.tsx`; das Übersichts-Skeleton (`trips/[id]/loading.tsx`) dient zugleich als zentrierter Fallback für `settings`/`prepayments` (kein eigenes loading.tsx). Wiederverwendbarer Dialog `components/modal.tsx` (Fokus-Trap/Escape/Restore, `max-h-[90dvh]`, optionaler `role`/`describedBy`-Prop). Darauf baut `components/confirm-dialog.tsx` (`useConfirm()`-Hook, promise-basiert, `role="alertdialog"`, **Initialfokus auf „Abbrechen"**, Gefahr-Variante) — ersetzt `window.confirm` bei destruktiven Aktionen (Buchung/Trip/Kategorie/Crew löschen, DSGVO-Purge) und den Settlement-Mail-Bestätigungen; der Fat-Finger-Betrags-Check im Buchungsformular bleibt bewusst nativ.
- **Globaler Client-Toast:** `components/toast-provider.tsx` (`useToast()`, `aria-live="polite"`-Host, Auto-Dismiss ~4 s mit Hover/Focus-Pause), in `app/layout.tsx` um `#main-content` gewrappt — für Feedback **ohne** Redirect (Schulden-Häkchen, Realtime-Hinweis, Mail-Teilfehler). Der URL-getriebene `components/toast.tsx` (`?toast=…`) bleibt parallel für Redirect-Flows. `RealtimeTrip` bündelt `router.refresh()` per 500 ms-Debounce und zeigt bei Änderungen **anderer** Crewmitglieder **tabellenspezifische** Toasts: „Eine Buchung wurde von … geändert." (`transactions`/`created_by`) bzw. „Der Schulden-Status wurde von … aktualisiert." (`settled_debts`/`settled_by_person_id`). **`trip_members`-Änderungen lösen KEINEN Toast mehr aus** (nur stilles Refresh) — die Tabelle trägt kein Akteur-Feld, weshalb der frühere generische „Von einem Crewmitglied aktualisiert"-Toast fälschlich auch beim **eigenen** Crew-Anlegen feuerte. Der Wortlaut respektiert den Reise-Typ (`tripType`-Prop → „Crewmitglied" / „Mitglied der Reisegruppe").
- **Explizite Data-API-GRANTs (Migration 0022):** Supabase deprecatet ab 30.10.2026 die automatischen `GRANT`s auf neue Tabellen im public-Schema. Migration `0022_explicit_grants.sql` setzt deshalb für alle bestehenden Tabellen/Views/`simplify_debts()` explizite `GRANT … TO authenticated` und definiert via `ALTER DEFAULT PRIVILEGES` Defaults für künftige Tabellen. **Konsequenz für neue Migrationen:** `CREATE TABLE foo …` reicht aus, der GRANT kommt automatisch — kein manuelles `GRANT` pro Migration nötig. RLS-Policies aus 0004 filtern weiterhin pro Row.
- **Anzahlungs-Modul** (Migrationen 0023–0028, Spec `docs/prepayments.md`): Mechanik für Yachtanzahlungen, die der Skipper Monate vor Törn-Start an die Charteragentur leistet und sich von der Crew rückerstatten lässt.
  - **Wizard** unter `/trips/[id]/prepayments/setup` (zwei Schritte): Aufteilung (`gleichmaessig` / `zeitanteilig` / `individuell` / `kojen`) + Vorstrecker-Auswahl + optional Kojen-Editor + WhatsApp-Vorlage; Schritt 2 = Tranchen-Liste mit Fälligkeit + Prozent (Σ = 100). Crew kann inline im Wizard angelegt werden (`CrewQuickAdd`). **Tranchen-Bezeichnung wird automatisch durchnummeriert** (`trancheLabel(index, total)` → alle bis auf die letzte „N. Anzahlung", die letzte „Endzahlung"; bei 1 Tranche → „Endzahlung") — kein freies Label-Feld mehr (schlankeres Design); das Label wird beim Speichern abgeleitet und in `prepayment_tranches.label` geschrieben. **Validierung:** „Weiter" (Schritt 1) ist bei Gesamtsumme 0 deaktiviert (für `gleichmaessig`/`zeitanteilig`), „Fertig stellen" (Schritt 2) bei Σ % ≠ 100. Editor-Zeilen stapeln auf Mobile (`grid-cols-1 sm:grid-cols-12`).
  - **Matrix** unter `/trips/[id]/prepayments`: Person × Tranche mit Statussymbolen (○ offen, ◐ teilweise, ✓ bezahlt, ⏰ überfällig, ⏳ pending). Klick auf Zelle → Modal zur Zahlungs-Erfassung mit Überzahlungs-Splitting auf zweite Tranche. Pro Person-Zeile 🔔 (Erinnerungs-Mail) und 💬 (WhatsApp-Text). Sammelnachricht-Button oben. Charter-Reminder-Banner für den Vorstrecker zeigt, was er an den Vercharterer noch überweisen muss (mit Überfälligkeits-Indikator). Vorstrecker-Zeile trägt blaues „Streckt vor"-Badge, Bell/WhatsApp-Buttons sind in dieser Zeile deaktiviert.
  - **Vorstrecker:** wer streckt die Yachtanzahlung vor (Default = Trip-Skipper, im Wizard editierbar). _Sprachregelung:_ „Vorstrecker" ist der interne Konzept-Name (Code-Identifier `advancer_*`, `requireSkipperAdminOrAdvancer`); in **Endnutzer-Texten (UI/Mail)** wird die Rolle geschlechtsneutral formuliert — Badge „Streckt vor", sonst „die vorstreckende Person" bzw. Verb „vorstrecken". Migration 0024 relaxed `tx_credit_self` für tranche-getaggte Credits, damit der Vorstrecker seinen eigenen Anteil als Selbst-Verrechnung abhaken kann (bilanzneutral). Der Vorstrecker darf abhaken, bestätigen, ablehnen und Erinnerungen verschicken — auch wenn er nicht Trip-Skipper ist (`requireSkipperAdminOrAdvancer` in `lib/auth/authz.ts`).
  - **Phase 2 — Crew-Selbstmeldung** (Migration 0025): Crew klickt in `/prepayments` „Ich habe gezahlt" → Eintrag mit `transactions.confirmed_at = NULL` (= pending). Vorstrecker bekommt eine Mail (`payment-pending-template.ts`) und sieht in der Matrix einen gelben Pending-Banner mit ✓ / ✗ Buttons. Der Banner-Eintrag zeigt nur „X hat Y € für N. Anzahlung gemeldet" + Datum — die Buchungs-Beschreibung wird bewusst NICHT angezeigt (redundant + teils irreführende Seed-/Default-Texte). Erst bei Bestätigung zählt der Eintrag in `v_prepayment_payments`. `transactions.confirmed_at` hat `DEFAULT now()` — normale Skipper-/Admin-Eingaben sind sofort bestätigt, nur `submitSelfPayment` schreibt explizit NULL.
  - **Crew-Fälligkeit 3 Tage vor Charter:** `prepayment_tranches.due_date` ist die Charter-Fälligkeit (gegenüber Vercharterer). Überall, wo die Crew die Frist sieht (Matrix-Header, Crew-Self-View, Erinnerungs-Mail, WhatsApp), wird über `toCrewDueDate()` in `lib/prepayments/dates.ts` ein 3-Tage-Puffer abgezogen. Der Charter-Reminder-Banner zeigt weiterhin das echte Datum. `toCrewDueDate` **clampt** zusätzlich: die Crewfrist rutscht nicht in die Vergangenheit, solange die Charterfrist noch aussteht, und liegt nie nach der Charterfrist selbst (`today` als Parameter überschreibbar für deterministische Tests).
  - **Bilanz Drei-Block-Übersicht** (in `/trips/[id]/balance` wenn ein Plan existiert): Anzahlungs-Pool / Bordkasse / Gesamt pro Person. Anzahlungs-Saldo = aktiv_beigetragen − soll (pragmatisch, nicht double-entry: credit_to des Vorstreckers zählt NICHT negativ bei ihm, weil treuhänderisch). Bordkasse-Saldo kommt aus separater View `v_balances_bordkasse_only` (Migration 0026, filtered auf `tranche_id IS NULL`). Untere Tabelle ist konsistent: zeigt nur Bordkasse-Buchungen wenn ein Plan existiert.
  - **Crew-Wechsel-Workflow:** `replaceMember` in `lib/actions/prepayments.ts` legt neue Person an, überträgt Obligation + Anwesenheit + Koje, erzeugt Gegen-Gutschriften für die bereits geleisteten Anzahlungen (B zahlt A privat aus → A geht auf 0 €, B rutscht in A's Position). Ghost-Crew ohne E-Mail-Adresse möglich. **UI:** ⇄-Icon pro Creweintrag in den Trip-Settings (`webapp/app/trips/[id]/settings/crew-section.tsx:ReplaceMemberForm`), disabled für den Original-Skipper (server-seitig ohnehin geblockt). War bis zu einem `knip`-Fund (Backend-Review 2026-08, siehe unten) **unverdrahteter toter Code** — beim Anbinden ans UI wurden drei Lücken gefunden und gefixt, die als Dead Code folgenlos, live aber Dateninkonsistenzen erzeugt hätten: (1) der Zahlungstransfer filterte nicht auf `confirmed_at` und hätte eine unbestätigte Selbstmeldung wie eine echte Zahlung übernommen — jetzt blockt ein Pre-Check den Wechsel, solange eine unbestätigte Selbstmeldung offen ist (Skipper muss erst in der Matrix bestätigen/ablehnen), statt sie zu übernehmen oder zu verwerfen; (2) eine `new_email`, die schon zu einem ANDEREN Crewmitglied DESSELBEN Trips gehörte, hätte dessen `trip_members`/`prepayment_obligations`-Zeile stillschweigend überschrieben und eine Gutschrift zwischen zwei unabhängigen Personen erzeugt — jetzt ein Block analog zum bestehenden `mergeGhostIntoExistingPerson`-Pre-Check in `trip-members.ts`; (3) mehrere Schreibschritte (Obligation-Transfer, Zahlungstransfer, Anwesenheit-auf-null) prüften ihren Supabase-`error` nicht — ein Fehler mittendrin hätte einen Teilzustand hinterlassen, während die Action trotzdem `ok` zurückgab; jetzt fail-loud mit früher Rückkehr. Ghost-Merge-Sonderfall (real und Ghost bereits beide Crew desselben Trips) bleibt wie gewohnt vom bestehenden `mergeGhostIntoExistingPerson`-Pfad in `lib/actions/trip-members.ts` abgedeckt (separater Workflow für Inline-Edit-Merges).

**Idempotenz (Fund 3, in einer Folge-Session gefixt):** ein Netzwerk-Retry bei flakey Yacht-WLAN (analog `0005_idempotency.sql`) hätte sonst die neue Person + den Anzahlungs-Transfer duplizieren können. Fix: das Formular generiert `new_person_id` client-seitig einmal pro Mount (`crypto.randomUUID()`, gleiches Muster wie `idempotencyKey` in `transaction-form-parts.tsx`) und schickt es als Hidden-Feld mit; der Server macht daraus über `.upsert({ id: … }, { onConflict: "id" })` sowohl `persons` als auch `persons_private` idempotent (Client-generierte PK statt reinem `insert`). Der Zahlungstransfer nutzt `idempotency_key = <ID der Quell-Zahlung>` auf der bestehenden UNIQUE(trip_id, idempotency_key)-Spalte (0005) statt eines eigenen Check-before-Insert — echte DB-Garantie statt TOCTOU-Race, exakt das Muster aus `recordPayment`/`insertCredit`. **Ein erster Entwurf mit Check-before-Insert auf Betrag+Datum wurde beim Grill-Review verworfen:** er hätte zwei unterschiedliche, zufällig gleich hohe Zahlungen vom selben Tag als „schon übertragen" verwechselt und eine davon verschluckt (Geld verschwindet aus der Bilanz) — `idempotency_key` pro Quell-Zahlung ist präzise, weil jede Zahlung eine eigene ID hat. Der Grill-Review fand beim ersten Idempotenz-Entwurf zusätzlich zwei eigenständige Bugs, die mitgefixt wurden: (a) der Pre-Check auf eine offene Selbstmeldung hing NACH der Personen-/Crew-Anlage — ein Reject hätte trotzdem einen Ghost-Crew-Orphan hinterlassen; läuft jetzt als Erstes, vor jeder Schreib-Operation. (b) die Invite-Mail war über ein lokales `wasCreated`-Flag gegated, das bei einem Retry-nach-Absturz (Person schon angelegt, aber Mail noch nicht verschickt) die Mail für immer verschluckt hätte; ersetzt durch einen `auth_user_id IS NULL`-Check auf die Zielperson (retry-sicher: eine Ghost-Person ohne Login bleibt NULL, bis sie sich tatsächlich einloggt). **Bekannte, bewusst akzeptierte Lücke:** landet `new_email` auf eine bereits existierende Person, die schon Crew dieses Trips ist (Fund-1-Guard), schlägt ein echter Retry NACH einem bereits erfolgreichen ersten Versuch mit der Kollisions-Fehlermeldung fehl statt idempotent No-Op zu sein — sicher (kein Doppel-Merge), aber nicht idempotent; ein Fix dürfte sich NICHT auf `new_person_id` als Vertrauensanker stützen (der Wert ist client-kontrolliert, sonst ließe sich der Fund-1-Guard per manipuliertem Hidden-Feld aushebeln). Regressionstests in `__tests__/replace-member.test.ts` (Mock filtert `.eq`/`.not`/`.is` tatsächlich und simuliert die reale UNIQUE-Verletzung, kein Vacuous-Pass); alle Fixes zusätzlich gegen die echte lokale Postgres-Instanz verifiziert (u.a. ein direkter Duplicate-Insert-Versuch mit demselben `idempotency_key`, der real an `idx_transactions_idempotency` abprallt).
  - **Tranche-Dropdown in normaler Buchungs-/Gutschrift-Form** (`webapp/app/trips/[id]/transactions/new/transaction-form.tsx`): optional eine Tranche zuordnen, dann landet die Buchung im Anzahlungs-Pool (taucht nicht im Bordkasse-Saldo auf). Die Yachtanzahlung wird so erfasst. **Auto-Vorbelegung (nur Ausgabe-Formular):** Tranche wählen füllt Betrag (= `plan.total_amount × percent / 100`, in beiden page.tsx `new`+`edit` vorberechnet und als `TrancheOption.amount` durchgereicht), Beschreibung (= Tranchen-Label) und Kategorie (= Törn-Kategorie mit „Yacht" im Namen, `/yacht/i`-Match im Form; fehlt sie — umbenannt/gelöscht/Reise-Typ „other" — bleibt die Kategorie unangetastet) vor; Datum bleibt heute. `CategorySelect` hat dafür einen optionalen Controlled-Modus (`selectedId`/`onSelect`); das Ausgabe-Formular hält die Kategorie als State. „Smart overwrite": nur leere oder noch-auto-gefüllte Felder werden gesetzt — manuell Eingegebenes bleibt, ein Tranchen-Wechsel aktualisiert, „Keine" leert nur die Auto-Werte; reine Logik in `lib/prepayments/tranche-autofill.ts:computeTrancheAutofill` (Vitest, `__tests__/tranche-autofill.test.ts`). Gutschrift-Formular: bewusst kein Autofill (Crew-Anteile laufen über die Matrix). **Tranche-Zuordnung serverseitig rollengeschützt (S-Fix, da Service-Role RLS umgeht):** `createExpense`/`updateExpense` verlangen für ein gesetztes/geändertes `tranche_id` zusätzlich zu `requireMember` ein `requireSkipperAdminOrAdvancer` — sonst könnte jedes Crewmitglied eine Ausgabe in den Anzahlungspool schieben, obwohl das UI-Feld (`canEditTranche`) nur Skipper/Admin/Vorstrecker sieht. `updateExpense` unterscheidet via `tranche_field_present`-Marker (von `TrancheField` nur gerendert, wenn das Feld sichtbar ist) „bewusst auf Keine gesetzt" von „Feld gar nicht angezeigt" → bei fehlendem Marker bleibt die bestehende `tranche_id` erhalten (kein versehentliches Lösen aus dem Pool durch nicht-berechtigte Editoren, z. B. den Ersteller). Gutschriften sind via `requireSkipperOrAdmin` ohnehin Skipper/Admin-only. Regressionstest: `__tests__/transaction-actions.test.ts`.
  - **Auto-Merge bei Crew-E-Mail-Kollision:** Wenn beim Inline-Edit einer Ghost-Crew eine E-Mail nachgetragen wird, die schon zu einem bestehenden Account gehört, mergt die Action den Ghost automatisch in den bestehenden Account (alle transactions, prepayment_obligations, trip_members, audit_log, settled_debts werden umgehängt). Pre-Check: ist die echte Person bereits Mitglied desselben Trips → Block mit Aufforderung, manuell aufzuräumen.
  - **A11y:** alle interaktiven Zellen tragen einen vollen Kontext-`aria-label` („Lucas, Endzahlung: bezahlt, 180,00 € von 180,00 € bezahlt"), Status-Emojis sind `aria-hidden="true"`. Pending-Banner ist `role="region"`. Touch-Targets ≥ 44px.
  - **Charter-Reminder-Mail an Vorstrecker** (`charter-reminder-template.ts`): wenn der 🔔-Button in der Vorstrecker-Zeile geklickt wird, schickt die App eine Charter-Übersicht statt einer persönlichen Tranchen-Liste — pro Tranche Soll Vercharterer, Σ Crew-Eingänge bei mir, schon-überwiesen, noch offen. `send-prepayment-reminder.ts` routet zwischen Crew-Pfad und Vorstrecker-Pfad anhand von `personId === advancer_person_id`.
  - **Auto-Reminder-Cron** (Migration 0028 + `/api/cron/prepayment-reminders`, in `vercel.json` als täglicher Cron `0 7 * * *` eingetragen): verschickt **innerhalb der letzten 6 Tage vor Charterfrist** eine Reminder-Mail an offene Crewmitglieder (`crew_3d`, ab 6 Tagen = 3 Tage vor Crewfrist) und **innerhalb der letzten 3 Tage** eine Charter-Übersicht an den Vorstrecker (`advancer_3d`). Fenster statt exakter Tag, damit ein verpasster Cron-Tag (Deploy, Outage) keinen Reminder verliert. Dedup über `prepayment_reminder_log(tranche_id, person_id, reminder_type)` — pro Kombi höchstens ein Eintrag. **Pending-Awareness:** Crewmitglieder mit unbestätigter Selbstmeldung (`v_prepayment_pending`) werden NICHT erneut gemahnt — sie haben ihren Teil getan und warten auf den Vorstrecker. **Advancer-Skip:** der Vorstrecker bekommt keine `advancer_3d`-Mail mehr, wenn er die Tranche bereits voll an den Vercharterer überwiesen hat. **Reject räumt Dedup-Log:** bei Ablehnung einer Selbstmeldung (`rejectSelfPayment`) wird der `crew_3d`-Log-Eintrag gelöscht, sodass der Cron einen korrigierten Reminder verschicken kann. Abgelaufene Trips übersprungen.
  - **Notice-Mails bei Admin-Aktionen** (`prepayment-notice-template.ts`, `sendPrepaymentNoticeMails` in `lib/actions/prepayments.ts`): wenn `recordPayment` / `confirmSelfPayment` / `rejectSelfPayment` von einer dritten Person (Admin/Co-Skipper) ausgelöst wird, gehen Info-Mails an Crew-Person + Vorstrecker (sofern der Actor nicht selbst Empfänger wäre). Self-Aktionen (Actor == Crew oder Actor == Vorstrecker) lösen keine Notice-Mail aus. Für Schulden-Häkchen (`sendDebtSettledMails`): bei Admin-Drittaktion bekommen Skipper UND Vorstrecker zusätzlich eine neutrale Observer-Mail, sofern sie nicht ohnehin Schuldner/Gläubiger sind.

**Mail-Design einheitlich** über [`lib/email/mail-shell.ts`](webapp/lib/email/mail-shell.ts) — `renderMailShell` rendert Logo-PNG-Header + Card-Wrapper + Footer, Helper `renderActionButton`, `renderHintBlock`, `escapeHtml`, `fmtEuro`. Alle sieben Templates (settlement, debt-settled, debt-observer, prepayment-reminder, charter-reminder, payment-pending, prepayment-notice) nutzen dasselbe Shell. Wero-Hinweis-Text dynamisch: „Bitte schicke {Vorstrecker} per Wero die fällige Anzahlung" — keine Klick-Links (Wero hat keine öffentliche API), Wero-ID + Verwendungszweck als Pille zum manuellen Übernehmen.

**Berechnungslogik:** in `webapp/supabase/migrations/0002_views.sql` (`v_transaction_shares`), `0003_functions.sql` (`simplify_debts()`), `0023_prepayments.sql` (`v_prepayment_payments`), `0025_self_payment_confirmation.sql` (`v_prepayment_pending`), `0026_bordkasse_only_balances.sql` (`v_balances_bordkasse_only`) `0031_per_person_no_alcohol.sql` (recreate `v_transaction_shares`) und `0032_balances_filter_deleted.sql` (recreate `v_transaction_shares` + `v_balances` mit Soft-Delete-Filter). **Bugfix 0031:** bei `split_type='per_person'` enthalten die Einzelbeträge den Alkohol bereits (Σ = `amount`) — ein zusätzlicher `alcohol_share` verteilte ihn doppelt (Saldo ≠ 0); jetzt für `per_person` auf 0 gezwungen (SQL **und** TS-Mirror `shares.ts`). ⚠️ Migration ursprünglich als `0029` angewandt, dann wegen Kollision mit `0029_trip_checklist` zu `0031` umbenannt — idempotentes `CREATE OR REPLACE VIEW`, erneut-anwenden unschädlich. TS-Mirror in `webapp/lib/calc/` — nur für Vitest, nicht im Render-Pfad. Anzahlungs-Soll-Berechnung in `webapp/lib/calc/prepayment-shares.ts` (`calculateObligations`). **Bugfix C-3 (Review):** `calculateObligations` verteilt bei `gleichmaessig`/`zeitanteilig` jetzt per **Largest-Remainder** (`allocateByWeights`, in Cent gerechnet), sodass Σ der Sollbeträge EXAKT dem Plan-Betrag entspricht — vorher rundete es pro Kopf (`round2(total/n)`) und Σ wich um Cents ab (z. B. 1000/3 → 999,99), der Vorstrecker sammelte einen Cent zu wenig ein. `individuell`/`kojen` bleiben Pass-through. Tests in `__tests__/prepayments.test.ts`. **Bugfix 0032:** soft-gelöschte Buchungen (`deleted_at IS NOT NULL`) zählten weiter in `v_balances` + `v_transaction_shares` → Bilanz-Seite und Saldo-Zeile der Abrechnungs-Mails waren nach jedem Löschen falsch (`v_balances_bordkasse_only` aus 0026, Transaktionsliste und Statistik filterten bereits, die ur­sprüngliche `v_balances` aus 0002/0015 wurde nie nachgezogen); jetzt `deleted_at IS NULL` in beiden Views. ⚠️ Diese Bug-Klasse lebt **ausschließlich auf SQL-Ebene** — der TS-Mirror (`lib/calc/balances.ts`) kennt `deleted_at` nicht und bekommt nur gefilterte Eingaben → Wächter ist der pgTAP-Test, nicht Vitest. **Bugfix 0042 (Review-Fund Q1):** `v_balances_bordkasse_only` (aus 0026) aggregierte die `credit_received`-`UNION ALL` (direkter Empfang + „An Alle") NICHT re-aggregiert wie `v_balances` — eine Person, die in BEIDEN Zweigen auftaucht (direkte Gutschrift UND „An Alle" von jemand anderem), erzeugte im `FULL OUTER JOIN` zwei Zeilen mit je vollem `paid`/`share`/`credit_given` → `simplify_debts` (0027 liest diese View, ohne `GROUP BY`) zählte doppelt → Phantom-Überweisungen, falsche Abrechnungs-Mails, `all_debts_settled` matchte nie → Törn unpurgebar. 0042 wickelt die `UNION ALL` in ein re-aggregierendes CTE (exakt wie `credit_received_per` in `v_balances`). Regression: `supabase/tests/bordkasse_credit_dedup_test.sql` (4 Checks: genau 1 Zeile/Person, `credit_received` summiert, Saldensumme = 0, korrekte Transfer-Anzahl). **Bugfix C-6 (Review, Tier 3):** `lib/utils.ts:round2` rundet halbe Cents jetzt VON NULL WEG (`Math.sign(cents) * Math.round(Math.abs(cents))`) — exakt wie Postgres `ROUND(numeric, 2)`. Vorher `Math.round` (Richtung +∞: −10,125 → −10,12 statt −10,13), wodurch der TS-Mirror `lib/calc/debts.ts:simplifyDebts` bei negativen .xx5-Salden vom SQL abwich. `debts.ts` importiert `round2` jetzt aus `lib/utils` (eine Quelle statt lokaler Kopie). Für positive Beträge unverändert. Test: `__tests__/calc.test.ts` (round2-Parität). **Aufräum-Runde (Tier 4, reine Qualität, kein Verhaltenswechsel):** geteilte Helfer statt Kopien — `formatAmount` (deutsches Komma ohne €) + `formatDeDate` leben in `lib/utils` bzw. `lib/prepayments/dates` (Kopien in Form/Wizard/Matrix/WhatsApp entfernt, `transaction-form-parts` re-exportiert sie für bestehende Importe); `fieldStr`/`fieldNum` (Outbox-formData-Dekodierung) in `lib/offline/outbox.ts` (genutzt von Pending-Karten + Draft-Editor); `mail-shell.ts:fmtEuro` delegiert an `formatEuro`; der Draft-Editor nutzt `parseRate` statt eigener `rateOrNull`-Logik. ⚠️ Die lokale `daysBetween` im Prepayment-Reminder-Cron wurde zu **`signedDaysUntil`** umbenannt (vorzeichenbehaftet + exklusiv) — bewusst NICHT mit `lib/utils.ts:daysBetween` (inklusiv `+1`, ≥0-geclampt) zusammenführen, ein naives Dedup verschöbe jedes Reminder-Fenster um einen Tag. `getPlan`/`getCharterPaidTotal` sind jetzt `React.cache()`-gewrappt (Per-Request-Dedup, Fund E-2); die Trip-Übersicht parallelisiert `countMyTransactions` + `getTripProgressSignals` (E-4). Die beiden Settlement-Mail-Buttons teilen sich `components/settlement-mail-button.tsx` (V-3, Browser-verifiziert). **Tier 4 Teil 2a (V1/V2):** die dreifach duplizierte Buchungs-Pipeline in `transactions.ts` entdoppelt — `expenseCommonInput`/`creditCommonInput` (die sicherheitsrelevanten `idempotency_key`/`tranche_id` bleiben BEWUSST explizit am Call-Site, so bleibt die S-1-Grenze „Replay übernimmt kein tranche_id" sichtbar), `insertParticipants`, `crewCountAtLeastTwo`. **Tier 4 Teil 2b (Mail-/Query-Effizienz):** `lib/email/send.ts` hat jetzt `sendMails(messages[])` — EIN gepoolter Transport (`pool`, `maxConnections:5`), pro Aufruf erstellt+geschlossen (serverless-sicher), verschickt Crew-weite Batches (Abrechnung `settlement.ts`, Schuld-Benachrichtigung `settled-debts.ts`) NEBENLÄUFIG statt N sequenzieller SMTP-Handshakes (E-1/E-5); die Debt-Push-Calls laufen via `Promise.all` (E-5). `getPrepaymentNavState` lädt tranches/obligations/payments/pending in EINER `Promise.all`-Welle statt hintereinander (E-3). Mailpit-verifiziert (Abrechnung: 3 Batch-Mails zugestellt; Schuld-Häkchen: rollen-korrekte Mail). **Noch offen (bewusst separater PR):** E-6 (Reminder-Cron N+1 — der saubere Fix erweitert die von zwei Pfaden geteilte `send-prepayment-reminder`-Helper um vorgeladene Daten; reine Cron-Latenz, kein Nutzer-Impact) + die 40×40-Header-Icons-Entscheidung.

**Wichtige Architektur-Entscheidungen:**
- **Schreib-Pfad:** Server Actions schreiben mit dem Service-Role-Client (`lib/supabase/admin.ts`), weil das User-JWT in Next.js 16 Server Actions die DB nicht zuverlässig erreicht. Auth/Authz wandert dadurch ins App-Layer (`lib/auth/authz.ts` mit `requireAuth/Skipper/Admin/SkipperOrAdmin/Member` + `isEmailAllowedToSignIn`).
- **Lese-Pfad:** alle Lese-Queries laufen über `lib/supabase/read-client.ts:readClient()`. Für globale Admins (in `ADMIN_EMAILS`) liefert es den Service-Role-Client → bypass RLS, damit Admins fremde Törns sehen. Für alle anderen User → Cookie-basierter Client mit aktivem RLS.
- **Request-Dedup + Client-Router-Cache (Tab-Ladezeit):** Zwei zusammengehörige Hebel gegen langsame Tab-Wechsel. (1) **`React.cache()`** auf `getCurrentPerson` / `isAdmin` (`lib/auth/`) + `getTrip` / `getTripMembers` (`lib/queries/trips.ts`): Diese laufen pro Seitenaufruf vielfach, weil JEDE Lese-Query über `readClient()` → `isAdmin()` → `getCurrentPerson()` geht (eine einzelne Tab-Seite löst ~6, Layout + Page beim Törn-Öffnen ~11 Aufrufe aus). Ohne Memoisierung war das pro Render ~6–11× `auth.getUser()` (**Netz-Call zum Auth-Server in Frankfurt**) + doppelt so viele `persons`/`persons_private`-Queries — reine Identitätsauflösung, noch vor den eigentlichen Daten. `cache()` dedupliziert das pro Request auf 1× (Bonus: schließt eine Erst-Login-Race beim parallelen Anlegen der `persons`-Row aus). Gilt nur INNERHALB eines Requests — kein Cross-Request-Caching, keine Stale-Gefahr. (2) **`experimental.staleTimes.dynamic = 30`** (`next.config.ts`): besuchte (dynamische) Törn-Tabs bleiben 30 s im Client-Router-Cache → Hin-/Herwechseln ist sofort, ohne Server-Roundtrip. `dynamic` (nicht `static`) greift, weil die Nav-Links (`bottom-nav.tsx`) kein explizites `prefetch` setzen und alle Törn-Seiten dynamisch sind (Cookie-Auth → kein Prerender). Sicher für die Finanzdaten: `router.refresh()` (RealtimeTrip bei Fremd-Änderung) UND `revalidatePath` (eigene Schreib-Actions) verwerfen den Cache sofort; die 30 s greifen nur für reines Navigieren ohne zwischenzeitliche Mutation. Bewusst NICHT angefasst (Reserve, falls Prod weiter hakt): SW-Stale-While-Revalidate für RSC, aktives Tab-Prefetch, Lade-Splash (Letzterer adressiert nur den Kaltstart, nicht die Tab-Latenz).
- **Cookie-Binding in Auth-Routes:** `/auth/verify` (POST) erstellt zuerst die Redirect-Response, dann `createClient(response)`. Der Cookie-Adapter schreibt Set-Cookie direkt auf die Response, sonst landen Session-Cookies nicht im Browser (siehe Supabase Discussion #35615).

**Auth-Email-Template:** liegt in `webapp/public/email/magic-link.html` — **einzige Quelle**. Die App liefert es öffentlich aus (Ausnahme für genau `email/magic-link.html` im `config.matcher` von `proxy.ts`), selbst gehostetes GoTrue holt es per `GOTRUE_MAILER_TEMPLATES_MAGIC_LINK` ab. ⚠️ Self-hosted GoTrue liest Templates **nur per HTTP-URL**, nicht aus gemounteten Dateien, und fällt bei nicht erreichbarer URL **still** auf sein unbrandetes Default zurück (kein Log-Eintrag) — nach Änderungen an Datei, Domain oder Matcher immer eine echte Testmail ansehen. Der frühere zweite Snapshot unter `supabase/email-templates/` ist entfernt (Dashboard-Pflege entfällt beim Self-Hosting, zwei Kopien wären gedriftet); dort liegt nur noch die README mit der Begründung des Token-Hash-Flows. URL-Pattern: `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email&email={{ .Email }}` (NICHT `{{ .Type }}` — Supabase rendert das bei Magic-Links als leeren String).

**Deploy:** derzeit Vercel mit Root Directory `webapp` (die DB ist schon selbst gehostet, siehe „Hosting-Stand"; für den Container-Betrieb `webapp/Dockerfile` + `docs/self-hosting.md`, Abschnitt 9a). Pflicht-Env-Vars: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL` (Production-Origin für Magic-Links; `lib/auth/origin.ts` **fällt auf `NEXT_PUBLIC_APP_ORIGIN` zurück**, wenn es fehlt — fail-loud nur, wenn BEIDE fehlen), `NEXT_PUBLIC_APP_ORIGIN` (gleicher Wert, von Mail-Templates verwendet UND als Origin-Fallback), `ADMIN_EMAILS`, `CRON_SECRET`, SMTP-Credentials (`SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASS` / `MAIL_FROM`). Für Web-Push zusätzlich `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` (einmalig via `npx web-push generate-vapid-keys`; **stabil halten** — Rotieren entwertet alle bestehenden Geräte-Abos → Versand 403). **DB-Migrationen laufen getrennt vom Vercel-Deploy:** nach dem Merge müssen neue Migrationen per `supabase db push` auf die Produktions-DB angewendet werden (Vercel deployt nur den App-Code) — sonst fehlen in Prod die neuen Tabellen/Funktionen und das jeweilige Feature bricht (so scheiterte anfangs das Speichern der Push-Abos, weil `push_subscriptions` in Prod noch fehlte).

**Env-Var-Status in Vercel** (Stand: 2026-06-04, laut Skipper-Screenshot — hier stehen bewusst nur **Namen + Scope, niemals Werte**; Snapshot bei Env-Änderungen aktualisieren):

| Variable | Scope in Vercel | Notiz |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | **nur Production** | Supabase-Endpoint (sensitiv) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | **nur Production** | RLS-Client-Key (sensitiv) |
| `SUPABASE_SERVICE_ROLE_KEY` | **nur Production** | Service-Role, bypasst RLS (sensitiv) |
| `ADMIN_EMAILS` | **nur Production** | Admin-Whitelist (sensitiv) |
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` | **nur Production** | Web-Push (privat/subject sensitiv) |
| `NEXT_PUBLIC_APP_ORIGIN` | Production + Preview | App-Origin (Mail-Links **und** Origin-Fallback in `lib/auth/origin.ts`) |
| `MAIL_FROM` | Production + Preview | Absender-Adresse |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASS` | Production + Preview | App-SMTP für Settlement-/Reminder-Mails (`SMTP_PASS` sensitiv). Hinweis: **Invite-/Magic-Link-Mails laufen NICHT hierüber**, sondern über Supabase-Auth-SMTP (Dashboard) |
| `CRON_SECRET` | Production + Preview | Cron-Auth (sensitiv) |
| `NEXT_PUBLIC_SITE_URL` | **NICHT gesetzt** | Magic-Link-Origin. Seit dem Origin-Fallback-Fix nicht mehr nötig (Code nimmt ersatzweise `NEXT_PUBLIC_APP_ORIGIN`); davor ließ das Fehlen **jeden Crew-Invite in Prod abstürzen** |

**Konsequenzen fürs Debugging & Code-Review (wichtig!):**
- **Preview-Deployments haben KEINE Supabase-Verbindung** — `NEXT_PUBLIC_SUPABASE_URL` / `ANON_KEY` / `SERVICE_ROLE_KEY` sind Production-only. Auf Preview-URLs ist daher alles Datenbezogene (Login, Trips, Buchungen, Bilanz) tot. „Läuft in Prod, bricht im Preview" ist meist **genau das** — kein Code-Bug.
- Auf Preview fehlen ebenso `ADMIN_EMAILS` (kein Admin-Bypass/Read-Bypass) und die VAPID-Keys (kein Push).
- Bei neuen Pflicht-Env-Vars immer prüfen, ob sie auch im **Preview**-Scope gesetzt werden müssen — sonst brechen Preview-Builds/-Tests an Stellen, die in Prod laufen.

**Tests:** Vitest (`__tests__/calc.test.ts` + `schema.test.ts` + `prepayments.test.ts` + `prepayment-dates.test.ts` + `outbox.test.ts` + `transaction-actions.test.ts` + `currency.test.ts` (Fremdwährung: Umrechnung, Kurs-Abruf, Schema) + `trip-vocab.test.ts` (tripVocab + `displayNameFromEmail`) — DOM-/IndexedDB-Tests via `happy-dom` + `fake-indexeddb`; `auth-url.test.ts` deckt zusätzlich den `resolveOrigin`-Env-Fallback `SITE_URL ?? APP_ORIGIN` ab) gegen S1–S7, das Spec-Szenario „Yachtanzahlung mit Kojen" sowie Edge-Cases (An-Bord-0-Anwesende, Pro-Person+Alkohol, An-Alle-N=2, `toCrewDueDate`-Clamp); Playwright (`e2e/smoke.spec.ts`) für öffentliche Routes + Auth-Schutz + Security-Header. **pgTAP** (`webapp/supabase/tests/*.sql`, ausführbar via `pnpm test:db` → `supabase test db`) prüft die echten SQL-Views/Funktionen (`v_transaction_shares`, `v_balances`, `simplify_debts`, `v_prepayment_payments`) inkl. Soft-Delete-Regression, die Bordkasse-Credit-Dedup-Regression (`bordkasse_credit_dedup_test.sql`, Fund Q1) sowie die View-RLS-Härtung (`view_security_invoker_test.sql`: `security_invoker` gesetzt, `anon` ohne `SELECT`, nicht-Mitglied sieht 0 Zeilen) — läuft **lokal via `pnpm test:db` UND in CI** (eigener `db-tests`-Job in `.github/workflows/webapp-ci.yml`, der per `supabase start -x studio,…` nur die DB-Container hochfährt statt des vollen Stacks — gegen die frühere Runner-Flakiness; pgTAP (SQL) + Vitest (TS-Mirror) bilden zusammen den Drift-Guard). Zusätzlich `webapp/supabase/_e2e_tests.sql` — ein self-asserting Integrationstest (61 Checks gegen die Live-Views, BEGIN/ROLLBACK), aufrufbar via `docker exec … psql … < supabase/_e2e_tests.sql`.

⚠️ **Falle — Seed-/Test-UUIDs müssen RFC-4122-valide sein:** Zod v4 `.uuid()` ist **strikt** (prüft Versions-Nibble + Variant-Bits). Platzhalter-IDs wie `aaaaaaaa-0000-0000-0000-…` (Version 0, Variant 0) werden abgelehnt → Schreib-Actions (`savePrepaymentPlan`, `recordPayment`, `createExpense` mit `paid_by`, …) scheitern mit **„Ungültige Auswahl."**, sobald eine geseedete ID validiert wird. Produktion ist nicht betroffen (`gen_random_uuid()` liefert echtes v4). **Konvention für Seeds:** Versions-Nibble `4`, Variant `8`, z. B. `aaaaaaaa-0000-4000-8000-000000000001`. Alle Seed-/Smoke-Dateien (`seed_demo.sql`, `seed_prepayments_test.sql`, `seed.sql`, `_smoke_tests.sql`) folgen diesem Format.

## Projekt-Dateien

```
.
├── CLAUDE.md                            # Dieses Briefing
├── docs/
│   ├── design-system.md                 # Komplettes Segel-Design
│   ├── calculation-rules.md             # Alle Aufteilungslogiken im Detail
│   ├── apps-script-reference.md         # Apps Script Funktionen + Zell-Mapping
│   ├── buttons-setup.md                 # Klickbare Speichern-Buttons in Google Sheets einrichten
│   ├── protection-setup.md              # Tabellenblätter schützen, Eingabefelder offen lassen
│   ├── prepayments.md                   # Anzahlungs-Modul Spec (Tranchen, Wizard, Matrix, Selbstmeldung)
│   ├── foreign-currency.md              # Fremdwährungs-Modul Spec (Kurs, EUR-Umrechnung, Bankkurs, Rechner)
│   ├── push-notifications.md            # Web-Push Spec
│   └── web-app-spec.md                  # Migrations-Spezifikation (historische Roadmap)
├── scripts/
│   ├── migrate_v8_to_v9.py              # Reproduzierbarer xlsx-Umbau v8→v9
│   └── migrate_v9_to_v10.py             # Reproduzierbarer xlsx-Umbau v9→v10
├── webapp/                              # Web-App (Next.js + Supabase) — Setup in webapp/README.md
│   ├── app/                             # App Router (Trips, Auth, Profile, Stats, Cron, About)
│   │   ├── about/                       # Public Funktionsübersicht mit Screenshots
│   │   └── trips/[id]/prepayments/      # Anzahlungs-Modul: Matrix, Wizard, Crew-Self-View
│   ├── components/                      # bottom-nav, realtime-trip, offline-banner, sw-register,
│   │                                    # modal, confirm-dialog (useConfirm), toast-provider (useToast)
│   ├── lib/                             # supabase, auth (mit authz), actions, queries,
│   │                                    # calc, validation, categories/icons, offline, db/audit,
│   │                                    # prepayments/ (dates, whatsapp), email/ (templates + send)
│   ├── public/                          # Logo, Manifest, Service Worker, robots.txt
│   │   └── about/                       # 19 Mobile-Screenshots (WebP) für /about-Seite (15-17 Anzahlungen, 18 Törn-Fortschritt, 19 Fremdwährung)
│   ├── scripts/                         # take-screenshots.ts (Playwright) + seed-demo.sh
│   ├── supabase/                        # config.toml + migrations + email-templates + seed
│   │                                    # + seed_demo.sql (3 Trips, Daten dynamisch aus CURRENT_DATE: Ostseetörn=laufend, Kroatien=Vorjahr, Korsika=Folgejahr+Anzahlung)
│   │                                    # + seed_prepayments_test.sql (Trip mit Crew für Anzahlungs-Tests)
│   │                                    # + tests/ (pgTAP: calc_views, balances_soft_delete, bordkasse_credit_dedup, login_rate_limit, view_security_invoker) → pnpm test:db
│   │                                    # + _e2e_tests.sql (self-asserting Live-View-Integrationstest)
│   ├── __tests__/                       # Vitest (calc, schema, prepayments, dates, outbox, transaction-actions; happy-dom+fake-indexeddb)
│   └── e2e/                             # Playwright Smoke-Tests
├── .github/workflows/webapp-ci.yml      # CI für webapp/ (lint + typecheck + test)
└── assets/
    ├── sheets-current/
    │   ├── Bordkasse_IJsselmeer2026_v10.xlsx    # produktive xlsx
    │   ├── Bordkasse_AppScript_v11.js           # produktives Script
    │   ├── Bordkasse_AppScript_v10.js           # v10-Script-Referenz
    │   ├── Bordkasse_IJsselmeer2026_v9.xlsx     # v9-Referenz
    │   ├── Bordkasse_AppScript_v9.js            # v9-Referenz
    │   ├── Bordkasse_IJsselmeer2026.xlsx        # v8-Referenz
    │   └── Bordkasse_AppScript_v8.js            # v8-Referenz
    └── design/
        └── (Logo, Bilder werden ergänzt)
```

## Wichtige Prinzipien

**Pragmatik vor Perfektion.** Die Sheets-Lösung wurde gewählt weil sie ohne Login funktioniert und die Crew sie sofort nutzen kann. Eine Web-App wäre technisch sauberer, hätte aber höhere Einstiegshürde.

**Deutsch ist verbindlich.** Auch in Kommentaren, Variablen optional englisch.

**Mobile zuerst.** Nicht-technikaffine Crew sitzt auf der Yacht mit Smartphone in der Hand, nicht am Laptop. Touch-Targets ≥ 40px Höhe, große Schrift, vertikal gestapelt.

**Fail-Safe.** Lieber ein Feld zu viel ausblenden als die Crew mit ungültigen Optionen verwirren. Apps Script validiert vor dem Speichern.

**Audit-Trail.** Transaktionen sind append-only — niemand löscht versehentlich Daten. Korrekturen über neue Gutschrift-Einträge.

**Barrierefreiheit mitdenken.** Bei jeder Änderung in der Web-App (`webapp/`) als Grundlinie WCAG 2.1 AA anstreben:
- Farbe ist nie alleiniger Informationsträger (z. B. Bilanz nutzt Farbe **und** „+/−"-Zeichen + Screenreader-Text).
- Jeder interaktive Bereich ist per Tastatur erreichbar und hat sichtbaren Fokus-Indikator (`focus:ring-2 focus:ring-primary/20` ist der Default).
- Custom-Komponenten folgen ARIA-Patterns (Listbox mit `aria-activedescendant` + Pfeil-Navigation, kein eigenes Wheel neu erfinden).
- Skip-Link in `app/layout.tsx` zum `#main-content`-Wrapper; `<html lang="de">` ist gesetzt.
- `prefers-reduced-motion` wird in `globals.css` respektiert — neue Animationen brauchen kein eigenes Handling.
- Touch-Targets mindestens 44 px (Default via `--spacing-touch` in `globals.css`).
- Form-Inputs haben Label-Verknüpfung (`htmlFor` / `id`); Fehler-Meldungen tragen `role="alert"`, Erfolgs-Meldungen `role="status"`.
- Bilder bekommen aussagekräftige `alt`-Texte, dekorative Icons `aria-hidden`.

Vor dem Mergen einer neuen Komponente kurz prüfen: kann ich nur mit Tab + Pfeiltasten alles bedienen? Sieht der Fokus-Ring sichtbar aus? Liest VoiceOver/TalkBack sinnvolle Texte?

**Grill-Pflicht vor Fertigmeldung.** Bevor eine nicht-triviale Code-Änderung (neues Feature, Bugfix, Migration, Refactor) als abgeschlossen gemeldet oder gemerged wird, muss sie zusätzlich zur eigenen Prüfung durch einen **unabhängigen Subagenten ohne Konversationskontext** adversarisch gegengelesen werden („Grillen") — nicht nur die eigene Selbstprüfung. Der Review-Agent bekommt ausschließlich den Diff/die betroffenen Dateipfade + genug Hintergrund, um kalt zu urteilen, und den Auftrag, aktiv nach Bugs, Security-Lücken und Edge Cases zu suchen statt Korrektheit anzunehmen. Trivial-Änderungen (Typo, Umbenennung, reine Doku) sind ausgenommen. Gefundene Probleme werden gefixt und wo sinnvoll durch einen Test abgesichert, bevor der Punkt als erledigt gilt. Hintergrund: ein solcher Grill-Durchlauf hat bereits einen Bug gefunden, den die Erstimplementierung UND ein `pnpm test`-Grünlicht übersehen hatten (fehlende Unterscheidung zwischen „anonymisiert" und „An Alle" bei Gutschriften nach dem DSGVO-Purge, siehe Migration 0046).

## Anweisungen für Claude Code

Wenn du in diesem Projekt arbeitest:

1. **Lies erst die Doku in `docs/`**, bevor du Änderungen machst — besonders `apps-script-reference.md` für Zellkoordinaten.
2. **Bei Sheets-Änderungen:** Datei in `assets/sheets-current/` direkt editieren (oder Kopie mit neuer Versionsnummer anlegen).
3. **Bei Apps Script-Änderungen:** Versionsnummer im Header inkrementieren (`v11` → `v12`), Changelog am Anfang der Datei aktualisieren.
4. **Bei strukturellen Änderungen** (neue Spalten, neue Tabs): IMMER alle Formel-Referenzen prüfen — siehe Pattern in den Recovery-Scripts in `docs/`.
5. **Testen mit den Szenarien** in `docs/calculation-rules.md` — alle 7 müssen weiterhin korrekt rechnen.
